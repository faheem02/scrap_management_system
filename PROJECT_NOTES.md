# SCRAP MANAGEMENT SYSTEM - Scrap Business Management System
<!-- ============================================================
THIS FILE IS THE PERSISTENT PROJECT MEMORY.
If opencode session restarts: READ THIS FILE FIRST, then continue.
Update this file whenever requirements change.
============================================================ -->

## SCRAP PROJECT (this project) — READ FIRST
- Project: **Scrap Management System** at `C:\xampp\htdocs\scrap_management_system` → `http://localhost/scrap_management_system/`. Converted copy of Mehboob Traders (kept as scratch business). DB: `scrap_management_system` (user root, no pw).
- Branding: "Scrap Management System" / "Scrap Business" everywhere (login, all printable letterheads, footers; sidebar is runtime-based).
- **Business differs from Mehboob Traders**: scrap party records (supplier/customer) carry **Plant Area + Plant Name** fields; auto-filled (read-only) when the party is picked in purchase/sale forms. `customers.area` column does NOT exist (removed).
- **Simple units only**: qty × rate = subtotal on purchases/sales/product rows. NO cartons / loose boxes / `boxes_per_carton` anywhere. `products.unit` defaults `'kg'` (dropdown: kg/ton/pcs/gadda/pack/dozen/meter/liter). Stock is DECIMAL(12,2) plain units.
- **Environment**: PHP `C:\xampp\php\php.exe` | MySQL `C:\xampp\mysql\bin\mysql.exe` (root, no pw, db `scrap_management_system`). Login `admin/admin123`. Default bank account + 6 expense categories seeded. All 22 tables 0 rows at baseline.
- **Past-history caveat**: the build-order/history notes below were copied from mehboob_traders and describe its carton/box business. Items #65 & #66 here are scrap-specific (purchase + sales module rewrites). Ignore item #64 (that was the mehboob expenses page change, not scrap). Items numbered 1–63 describe mehboob_traders and do NOT apply verbatim to scrap (esp. anything about `boxes_per_carton`, cartons, `customers.area`).
- Verification: all 71 PHP files lint clean; global grep clean for Mehboob/boxes_per_carton/scrab in code; 11 key pages + 5 extras render HTTP 200 with Scrap branding (item #67).

## BUSINESS OVERVIEW (from client - Aug/Sep 2026)
- Name: **Mehboob Traders**
- Type: **Wholesale (Whole Sale) business** — Parchoon-type shop items
- Sale items: **biscuits, bubbles/balloons, goliya (toffees), aata (flour), pills/pops, Nimko, etc.**
- Basically: Wholesale supplier of every item you find in a Parchoon shop
- Work happens at wholesale level (bulk qty, wholesale rates)

## WHAT CLIENT WANTS (build order)
1. **Dashboard** (`index.php`) - DONE (rebuilt professional, verified with demo data 08-Sep-2026). Shows (English labels):
   - **Greeting bar**: time-based greeting (Good Morning/Afternoon/Evening) + full date + quick action buttons (New Sale, Purchase, Pay Amount, Receive).
   - **KPI Row 1 (money)**: Today's Sales (+invoice count), Today's Purchases (+purchase count), Cash in Hand (+/- today cash in/out), Bank Balance (+account count). Colored icon circles.
   - **KPI Row 2 (position)**: Customer Receivable (to receive, link), Supplier Payable (to pay, link), Today's Expenses, Low/Out of Stock count + stock value. Total 8 stat cards.
   - **Charts**: 7-day Sales vs Purchases bar chart (Chart.js 3.9.1 CDN) + Expense by Category doughnut (this month) with custom legend. Empty state handled ("No expenses recorded this month").
   - **Recent tables**: Recent Purchases + Recent Sales (Invoice/Supplier-Customer/Total/Due).
   - **Low Stock Alerts table** (product, category, in-stock, min level, status badge) + **Recent Cash Book activity** list (inflow green/outflow red with +/- amounts).
   - **Chips row**: Products, Customers, Suppliers, Employees counts (clickable).
   - New CSS classes in style.css: greet-bar, stat-label/value/sub, icon-circle (+ color variants), activity-list/item/dot, exp-legend-item, chip-chip, small-box + bg-slate/emerald/blue/orange.
2. **Purchase** module (dropdown) - DONE (with product dropdown)
3. **Product** - ALAG dropdown (Products/Categories/Brands) + **stock management** - DONE
4. **Supplier** module - DONE
5. **Customer** module - DONE
6. **Expense** module - DONE
7. **Cash Book / Bank Book** modules - DONE
8. Sales module - ADDED
9. **Pay / Receive** - DONE (AB SIDEBAR DROPDOOS KE ANDAR as client's final clarification). NO separate top-level menu. Pages live INSIDE existing dropdowns:
   - **Suppliers ▾ dropdown** me "Pay Supplier" (`modules/transactions/pay_supplier.php`) — supplier ko paise Dene hain. Red theme. Supplier dropdown + balance + cash/bank + Confirm Payment. Neeche table "Suppliers with Balance (Aap Dain)" + quick Pay button (`?supplier_id=`).
   - **Customers ▾ dropdown** me "Receive Payment" (`modules/transactions/receive_customer.php`) — customer se paise Lene hain. Green theme. Neeche table "Customers with Balance (Aap Lain)" + quick Receive button (`?customer_id=`).
   Note: dropdown items ka naam sirf "Pay Amount" hai (icons lage hain - fa-money-bill-wave supplier, fa-hand-holding-usd customer; NO arrows) dono dropdown me.
   Verified: Pay 2000 (bal→cash out), Receive 1200. Header: `$on_customer` bhi checks receive_customer so dropdown stays open.

10. **Employees** module - DONE (3 categories: Salesman, Order Booker, Loader). `modules/employees/`:
    - `create.php` - Add Employee + **login username/password** (creates users row, role auto from employee_type). `edit.php` - can change username/password. `toggle_status.php` - toggles employee+user status together. `delete.php` - deletes user too.
    - `index.php` - list w/ stat boxes + filter + **Login User column**.
11. **Employee separate login + roles** - DONE (client: "employees ka alag sy user"). users.role ENUM extended ('salesman','order_booker','loader'); employees.user_id FK→users. Access matrix:
    - **Admin**: full access (sab kuch).
    - **Salesman**: Dashboard, Sales (New Sale/Invoices), Customers (View), Products (View). Sirf sales ka kaam.
    - **Order Booker**: Customer orders = Sales (same as salesman access).
    - **Loader**: Dashboard (limited), Products List only (View). Loader ko sirf inventory dekhna.
    - Server-side guards: `requireRole([...])` in functions.php (absolute redirect to /<app>/index.php). Applied to all module pages.
    - `modules/profile/index.php` - My Profile (change own password), topbar dropdown + role badge.
    - Dashboard role-aware: employees dekhte nahi finance (purchases/cash/bank/expenses), charts, recent purchases, cashbook. Sales team ko Today Sales + Recent Sales + Customers/Products chips. Loader ko sirf low-stock + Products chip.
    - Verified (Sep 2026): admin/salesman/loader sidebar + guards (blocked from pay_supplier/invoices with permission msg) all pass. Login: admin/admin123. Employee login: admin banayega.
16. **Customer/Supplier CNIC + Salesman Area (territory)** - client: "add customer or add supplier me cinic field, add customer me area bhi; jab salesman add kary gy to usko area allocate kary gy, phr usko bs wohi customers show houn gy jo us ke area ke hain".
    - DB (live + `database_schema.sql`): `customers.cnic VARCHAR(30)`, `customers.area VARCHAR(50)` (after city), `suppliers.cnic VARCHAR(30)`, `employees.area VARCHAR(50)`.
    - `includes/functions.php`: new `salesmanArea($pdo)` - returns logged-in salesman's area (employees.user_id = session user_id), null otherwise.
    - **forms**: customers.php add-modal → CNIC + Area/Town (list also shows Area column); suppliers.php add-modal → CNIC; employees create/edit → Area/Territory field + employee list Area column.
    - **salesman area filter** (only role salesman; admin/order_booker/loader unaffected): `customers.php` list, `sales/index.php` dropdown + POST guard, `sales/invoices.php`, `sales/ajax_customer_balance.php`, guards on `customer_view.php` + `sales/invoice.php` (redirect "not in your area").
    - Verified (Sep 2026): salesman "Johar Town" saw only Johar Town customer (list + dropdown); blocked Gulberg customer_view id + ajax balance; admin sees all. Test data cleaned.
17. **Customer/Supplier Notes + Edit/Delete/Ledger actions** - client: "customer/supplier notes jo add karty waqt likhy hain wo show karo + action column me edit/delete/ledger buttons".
    - `suppliers.notes TEXT` added (live + `database_schema.sql`). Both add forms have Notes; lists show Notes column.
    - customers.php + suppliers.php Action column: Edit (pencil), Delete (trash, inline confirm POST), Ledger (book → `*_view.php`). Edit/Delete admin-only (salesmen get only Ledger).
    - New pages: `customer_edit.php`, `supplier_edit.php` (profile + notes; balance is display-only, changed only via sales/payments), `customer_delete.php`, `supplier_delete.php`.
    - **Delete safety**: blocked with error if customer has sales/receipts or supplier has purchases/payments (aka has history). Otherwise deletes.
    - `customer_view.php`/`supplier_view.php` show Notes.
    - Verified (Sep 2026): add/edit/delete work; history-block works; ledger pages render. **Note:** accidentally deleted client's "Ahmad"/"ALi" records during delete-block test (they had only opening balance, no history) - restored exactly via activity log (customer_no CUS-2609-0001, opening 10000 / ALi 5000).
18. **Ledger: Pay links → transactions pages + search + print** - client: "receive/pay button ko transactions page sy link karo, search bar aur print button lagao".
    - `customer_view.php`: "Receive Payment" → `transactions/receive_customer.php?customer_id=` (preselect, admin-only). `supplier_view.php`: "Pay Supplier" → `transactions/pay_supplier.php?supplier_id=`. (Both transaction pages already support preselect.)
    - Added search bar (`#ledgerSearch`) + Print button (`window.print()`, uses existing `@media print` + `no-print`) on both ledger pages.
    - Sales + Received tables (customer) / Purchases + Payments tables (supplier) got IDs (`custSaleTable`, `custReceiptTable`, `supPurchaseTable`, `supPaymentTable`); search filters rows client-side on invoice/date/description.
    - Verified (Sep 2026): preselect renders (Ahmad on receive, ALi on pay), pages lint-clean.
19. **Receive Amount modal + payment history + customer search** - client: "receive_customer.php page top right corner pe 'Receive Amount' button; click pe popup modal me 'Receive from Customer' form; page pe sari payments show hon; search bar se customer search".
    - `modules/transactions/receive_customer.php` rewritten: inline form REMOVED → moved into Bootstrap modal (`#receiveModal`), opened by green "Receive Amount" button top-right; modal footer = Cancel + Confirm Receipt.
    - "Customers with Balance" list keeps "Receive" buttons which now open the modal with the customer preselected; `?customer_id=` preselect also auto-opens the modal (customer_view.php "Receive Payment" link still works).
    - New **All Payments** table (all `customer_receipts` JOIN customers + bank_accounts, newest first) with Method badge (Cash/Bank + account name).
    - Search bars: `#custSearch` filters balance list, `#paySearch` filters payment history (client-side, on name/date/description).
    - Verified (Sep 2026): created test customer + receipt via modal POST (302 redirect, cash_book + activity logged, PKR 1,234.50 row in history); cleaned all test rows.
20. **Opening Balance buttons (Cash Book + Bank Book)** - client: "cash book or bank book me opening balance ka button bhi do".
    - **Cash Book** (`modules/cashbook/index.php`): top-right "Opening Balance" button → modal (date, default today + amount). Sets/upserts `cash_book_daily.opening_balance` for that date, recomputes that day's closing and propagates opening/closing forward for all later dates (chain). Logs an `opening_balance` row in `cash_book` (shown in entries, never counted in inflow/outflow totals) + activity log.
    - **Bank Book** (`modules/bankbook/index.php`): top-right "Opening Balance" button → modal listing every account with its opening input. Saves `opening_balance` and recalculates `current_balance = opening + (deposits+transfer_in) - (withdrawals+transfer_out)` per account. Only accounts posted are changed; others keep stored value. + activity log.
    - Verified (Sep 2026): cash POST set 09-01 opening 50000 (daily row + cash_book entry), chained to 09-08 opening; cleaned. Bank test account got opening 75000 → current 75000; cleaned. Also fixed a leftover 09-08 daily row from an earlier deleted test receipt.
21. **Bank Book opening: date field like Cash Book** - client: "bank book opening balance same like cashbook, with date field".
    - Added `bank_accounts.opening_date DATE` (live DB + `database_schema.sql`). Bank opening modal now has a Date field (defaults today / last saved) + per-account opening inputs; save stores `opening_date` alongside each account's `opening_balance`, recalculates `current_balance = opening + deposits - withdrawals`.
    - Account cards on Bank Book show "Opening: PKR X (date)".
    - Verified (Sep 2026): opening 100000 on 2026-09-01 saved for Default Account → current 100000; reverted to 0/NULL after test.
22. **Bank Book: drop account cards, opening balance row in Bank Transactions** - client: "default amount card remove karo; opening balance Bank Transactions section ke first row me show karo".
    - Removed the per-account cards block from Bank Book.
    - Stat row = 3 cards (col-md-4): Total Deposits, Total Withdrawals, Total Bank Balance (opening NOT a stat card).
    - **Bank Transactions table first row**: "Opening Balance" badge + total opening (SUM bank_accounts.opening_balance) + latest opening_date (MAX opening_date); neutral amount (no +/-).
23. **Date filter on Cash Book + Bank Book** - client: "week/month transactions easily dekhny ke liye date filter laga do".
    - Toolbar (both pages): From/To date inputs + Filter button + All reset, plus preset links Today / This Week (Mon-Sun) / This Month.
    - Cash Book: filters `cash_book` entries table AND Total Inflow/Outflow stat cards (Cash in Hand stays latest daily closing).
    - Bank Book: filters `bank_transactions` table AND Total Deposits/Withdrawals stat cards (Total Opening/Total Bank Balance stay overall).
    - Default (no params) = All. Uses parameterized queries.
    - Verified (Sep 2026): range 01-09–08-09 shows only the 500 receipt + totals.
12. **Font family** - Poppins laga di (client ki request "achi font family"). `assets/css/style.css` body font-family + Google Fonts link in `includes/header.php` & `login.php` (Inter replaced by Poppins 400-700).
13. **Sidebar = gym-style (no collapse button)** - client: collapse button khatam, sidebar exactly gym wali. Desktop: fixed full-width sidebar always (no collapse/expand), `#sidebar-footer` = Logout link (gym jaisa). Mobile: off-canvas drawer (`translateX(-100%)`), `#sidebarOverlay` blur backdrop (z-1025), hamburger slide-in, overlay click / Escape / nav-link click closes drawer (submenu `#collapse*` toggles skipped). Removed all `.sidebar.collapsed` CSS + `#sidebarToggle` JS + `sidebar-toggle-area` HTML. `assets/js/main.js`: hamburger toggles overlay+sidebar. Verified render.
14. **New Purchase - Carton/Box system** - client: "jo purchase karta hai wo cartoon me karta hai". `modules/purchases/create.php` rewritten:
    - Per row: **Boxes/Carton** (prefill from product), **Cartons**, **Loose Boxes**, **Total Boxes (auto)** = cartons×bpc + loose, **Carton Total (auto)** = e.g. "10 Cartons + 5 Boxes", Rate per box, Subtotal auto.
    - Green summary line: Total Qty in Boxes + in Cartons, live.
    - Server: `qty = cartons*bpc + loose`; saved to `purchase_items` (new cols: `cartons`, `boxes_per_carton`, `loose_boxes`; `quantity` = total boxes). Stock += total boxes. `products.boxes_per_carton` saved/updated per product on purchase.
    - `products` table + `database_schema.sql`: `boxes_per_carton INT DEFAULT 1`. `product_create.php` gets "Boxes / Carton" input. `products.php` list shows Boxes/Carton + Stock (Boxes) columns.
    - Verified (Sep 2026): 10×48+5=485 boxes auto, stock 485; 2-item multi-row: 99 & 130 boxes, discount totals correct. Test data cleaned.
15. **All text English (no Roman-Urdu)** - client: "urdu use ghalat". Replaced all leftover Roman-Urdu labels: suppliers.php "Total Payable (Hum ne Udhaar)" → "Total Payable (We Owe)", "Advance (Supplier hum par dain)" → "Advance (Supplier Owes Us)". Also cleaned customers.php/customer_view.php/customer_receipt.php/sales (invoices/invoice/ajax)/receive_customer.php/pay_supplier.php (headers, toasts, empty-table msgs)/supplier_payment.php/product_create.php/products.php/purchase create/sale index/employee create+edit ("Udhaar"→"Due"/"Receivable", "Aata/Bechne"→Purchase/Sale Price, "Mal Bechna/Mal Entry" removed, "Sab clear hai"→"Everything is settled"). Full-project scan clean (no Urdu chars, no Roman-Urdu). All php -l clean (42 files).
    - Table `employees` added in database_schema.sql (id, emp_code, full_name, employee_type ENUM(salesman,order_booker,loader), phone, cnic, address, joining_date, salary, status, created_at, updated_at) + created live in DB.
    - Sidebar: Management section me "Employees" dropdown (Add Employee + View Employees).
    Verified: add 3 types, type filter, toggle status, delete ✓.

11. More modules coming later - client will tell step by step

## CORE REQUIREMENTS
- Website **MUST be responsive** (mobile + tablet + desktop)
- **Good color combination** — clean professional look
- Same project structure as `C:\xampp\htdocs\installment_business`
- Inside functionality is DIFFERENT from installment project (wholesale, not installment)

## TECH STACK (same as reference project)
- PHP 7.4+ / 8.0 (flat files, NO framework, PDO prepared statements)
- MySQL/MariaDB 10.4.32 (MariaDB on this machine)
- Bootstrap **4.6.2** (CDN), jQuery 3.6.0 (CDN), Font Awesome 5.15.3 (CDN)
- Flatpickr date picker (CDN)
- Custom style.css theme + main.js

## PROJECT STRUCTURE (follow reference project)
```
mehboob_traders/
├── config/db.php          # PDO connection (localhost, root, no pw)
├── includes/
│   ├── header.php         # sidebar + topbar + flash messages (single layout)
│   ├── footer.php         # scripts + flatpickr init
│   ├── functions.php      # helpers: getAll, getById, insert, update, delete,
│   │                      #   generateInvoiceNo, formatCurrency, formatDate,
│   │                      #   recordCashInflow/Outflow, recordBankInflow/Outflow
│   └── auth.php           # session check
├── assets/css/style.css   # custom theme
├── assets/js/main.js
├── modules/
│   ├── dashboard/ (or index.php at root)
│   ├── purchases/         # wholesale purchase entry + list
│   ├── suppliers/
│   ├── customers/
│   ├── expenses/
│   ├── cashbook/
│   └── bankbook/
├── database_schema.sql    # full schema mirroring the needs
├── seed_admin.sql         # default admin login
├── login.php / index.php
└── PROJECT_NOTES.md       # THIS FILE - persistent memory
```

## OFFLINE FRAMEWORK COPY (important for live/hosting)
- CDN links used in reference project. For live hosting always works.
- DB: use plain SQL, utf8mb4, InnoDB. No version-specific MySQL features
  so DB import works on any shared hosting (PHP 7.4+, MySQL 5.7+/MariaDB).

## DEFAULT ADMIN LOGIN (mirror reference)
- username: `admin` | password: `admin123` (seeded via seed_admin.sql)

## COLOR SCHEME
- CONFIRMED: **Slate + Emerald** — dark slate sidebar `#1e293b`, emerald accent `#10b981`.
- Inter font family, unique compact design (different from installment project).

## OPEN QUESTIONS / PENDING
- [x] Confirm color scheme / accent color for dashboard -> NAVY + AMBER CONFIRMED
- [x] Confirm branches/DB name -> default: mehboob_traders
- [x] Brand name shown in sidebar -> Mehboob Traders
- [x] Customers/Suppliers udhaar (credit) -> CONFIRMED both work on credit AND cash
- [x] Purchase cash/credit with supplier balance -> CONFIRMED yes
- [ ] Whether system needs tax/GST on wholesale rates -> (assume no tax unless told later)

## DECISIONS LOG
(keep appending every decision/changes client + developer agree on)
- 2026-09-08: Color scheme = Slate + Emerald (NOT navy+amber). Dark slate sidebar #1e293b, emerald accent #10b981. Inter font.
- 2026-09-08: Unique fresh design - NOT same as installment project. Compact sidebar + hover effects.
- 2026-09-08: Products ka ALAG dropdown (Products/Categories/Brands) - separate from Purchase dropdown.
- 2026-09-08: Stock management required - products have stock_quantity, min_stock_level, low-stock alerts.
- 2026-09-08: Both suppliers (purchase) and customers (sale) run on CREDIT + CASH (partial/full).
- 2026-09-08: Build order: Dashboard -> Purchase -> Supplier -> Customer -> Expense -> Cash/Bank Book -> Sales (added) -> Reports.
- 2026-09-08: End-to-end tested: purchase (stock+balance+cashbook), sale (stock-+balance+inflow), customer receipt (balance--inflow).

## BUSINESS FLOW NOTES
- Suppliers: mal dene walay. Purchase cash ya credit. Supplier balance = due on purchases - cash paid.
- Customers: retailers. Sale cash ya udhaar. Customer balance = due on sales - cash received.
- Both run on CREDIT + CASH (thora/pooora). paid_amount + due_amount on every purchase/sale.
- Unit tracking: pcs/dozen/box/carton/kg/pack/gadda.
- Stock: purchase adds stock, sale reduces stock. Low-stock & out-of-stock alerts.
- Cash flow: purchases/expenses/supplier payments = cash OUT. sales/customer receipts = cash IN.
- Bank flow: payment_method=bank uses bank account, else cash.
- Purchases have invoice_no PUR-yymmdd-###. Sales have INV-yymmdd-###.
- Customer_no format CUS-yymm-####.

## PROGRESS LOG
- [x] Base scaffold created (config/includes/assets/login/schema)
- [x] Full DB schema created (20 tables, wholesale)
- [x] Fresh Slate+Emerald design created (NOT same as installment)
- [x] Dashboard built (today's sales/purchases/expenses, customer udhaar, supplier udhaar, cash in hand, bank balance, low stock, recent purchases/sales)
- [x] Products module built (products list + add with stock + categories + brands)
- [x] Purchase module built (create with product dropdown + dynamic rows, stock updates, supplier balance, cash/bank book, purchase list)
- [x] Sale module built (new sale + invoice list + printable invoice, stock reduce, customer balance)
- [x] Suppliers module built (create/list/view ledger/pay supplier)
- [x] Customers module built (create/list/view ledger/receive payment)
- [x] Expenses module built (expenses + categories)
- [x] Cash Book module built (daily summary + entries + cash in hand)
- [x] Bank Book module built (accounts + transactions + balances)
- [x] Reports module built (sales/purchases/profit-loss with date filter)
- [x] End-to-end functional tests passed (purchase, sale, receipt, balances, cashbook)

24. **New Purchase: supplier + product search bars (autocomplete)** - client: "new purchase page me supplier field ko select sy hata k search bar lagao + suggestions; same product field (Carton/Box Entry) me bhi".
    - `modules/purchases/create.php`: supplier `<select>` REMOVED → text search `#supplierSearch` + hidden `supplier_id` + suggestion dropdown (name/phone/city). Product row select → `product-search` input + hidden `product_id[]` (works for each dynamic "Add Another Product" row). Suggestions appear as you type (250ms debounce), click/Enter picks, Escape closes, up/down arrows navigate, outside click closes.
    - New endpoint `modules/purchases/ajax_search.php`: `?type=supplier|product&q=` → JSON (LIKE on name/phone/contact_person/city for supplier; name/code for product). Suppliers searched on name+phone+contact_person+city; products on name+code. LIMIT 8 each. Auth = admin only.
    - On product pick: prefill Boxes/Carton + Rate/Box from the record; on supplier pick: keep existing `ajax_supplier_balance.php` hint (Current balance).
    - Form validation: supplier must be picked from suggestions before submit (error msg under field, mirrors old select `required`).
    - New CSS in style.css: `.ac-wrap/.ac-list/.ac-item/.ac-name/.ac-sub/.ac-empty` (absolute dropdown below input).
    - Verified (Sep 2026): lint clean; page renders (no old selects); supplier ajax returns ALi; product search returns a test product; full E2E purchase POST (1 carton × 48 = 48 boxes, stock 48, supplier balance 2400) → all test rows deleted (0/0/1 left = only client ALi supplier).

25. **Autocomplete selection fix + stale-cache fix** - client reported "suggestions show but can't select" on New Purchase. Debugged with a REAL Chromium (Edge headless) probe page in web root: reproduced the app's exact markup + inline JS and drove real mouse events — selection ACTUALLY WORKED (type → suggestion → mousedown click → value filled, no JS errors). Root cause = browser showing a heuristically-cached old page (XAMPP sends no cache headers, so Apache/browser heuristically cache `.php` output).
    - `includes/auth.php`: added `Cache-Control: no-store, no-cache, must-revalidate, max-age=0` + `Pragma: no-cache` to ALL authenticated pages. This kills stale-page serving permanently.
    - `includes/header.php` + `includes/footer.php`: bumped `style.css?v=2` / `main.js?v=2` (forces fresh theme after CSS/JS changes).
    - `modules/purchases/create.php` JS hardened regardless: shared `pickSupplier($item)`/`pickProduct($item)` functions; item handler now binds `'mousedown click'` (click is a fallback for browsers where mousedown alone didn't fire); Enter key now picks the first suggestion even if nothing is highlighted (before it needed an active item).
    - Verified (Sep 2026): all pages 200 after auth change; Chromium probe passes supplier + product full flow (suggestions render + real click selects), probe file `_ac_probe.html` DELETED after test. No test rows created (page-only changes).

26. **Pay Supplier page rebuilt like Receive Customer** - client: "jesa transactions/receive_customer.php bana hua wesa supplier k liye bhi bana do". Now `modules/transactions/pay_supplier.php` mirrors item 19 (receive_customer):
    - Top-right **"Pay Amount"** button (danger-red) → Bootstrap modal `#payModal` with Pay Supplier form (supplier dropdown + live balance hint, amount, date, method cash/bank + account, notes; footer Cancel + Confirm Payment).
    - "Suppliers with Balance (To Pay)" table (Supplier/Phone/City/Payable/Pay button) — Pay button opens modal preselected. Search `#suppSearch` filters rows.
    - **All Payments history table** (all `supplier_payments` JOIN suppliers + bank_accounts, newest first) with Method badge (Bank + account / Cash) + `#paySearch` filter (supplier/date/description).
    - `?supplier_id=` preselect ALSO auto-opens the modal (supplier_view.php "Pay Supplier" link still works).
    - Balance hint logic mirrors customer: bal>0 "Payable", bal<0 "Advance", else "Clear".
    - Sidebar fix: `includes/header.php` `$on_supplier` now also matches `pay_supplier` so the Suppliers dropdown stays open on this page (was `inventory/supplier` only).
    - Verified (Sep 2026): lint clean; render (button/modal/search/history + ALi context); temp supplier (opening 10000) → POST payment 2000 → supplier_payments row + cash_book outflow + balance 8000 + activity log; preselect `?supplier_id=11` auto-opens modal; **ALL test rows deleted** (supplier_payments 0, supplier 1 = ALi, 09-09 daily row removed — pre-existing 09-08 row + its 2 cash_book rows untouched).
27. **Receive Customer modal: select → search-with-suggestions** - client: "customers.php pe payment button se khulne wale customer select ko remove kar k search field lagao jesay purchases me hai". Now `modules/transactions/receive_customer.php` modal's `#partySelect` dropdown is replaced by an autocomplete search field EDGE-TO-EDGE mirroring `purchases/create.php` supplier search:
    - New `modules/transactions/ajax_customer_search.php` (`?q=`, name/phone/customer_no LIKE, LIMIT 8, returns id/name/phone/city/current_balance).
    - New `modules/transactions/ajax_customer_balance.php` (`?id=` returns formatted current balance).
    - Reused `.ac-wrap/.ac-list/.ac-item` CSS already in `style.css` (from change #24); same debounce timer, `mousedown click` binding, `ac-empty` row, keyboard up/down/enter/escape, click-outside close.
    - `?customer_id=` preselect (ledger "Receive Payment" link) still auto-opens modal AND fills the search box + balance hint (fetches via `pickCustomer`).
    - `.pick-party` rows in "Customers with Balance" table still open modal with that customer pre-filled.
    - Submit-blocking validation if customer not picked (hidden `customer_id` empty → `#customerError` shown).
    - Verified (Sep 2026): lint all 3 files; node --check JS passes; search `Ah` → Ahmad id 8; balance id 8 → 9,500.00; render shows search box + hidden id; preselect renders `pickCustomer(8, "Ahmad")` and modal opens; empty-customer POST → "Select a customer" validation. No test rows created (read-only change).
28. **Pay Supplier modal: select → search-with-suggestions** - client requested same treatment for `pay_supplier.php` as #27 (WORKFLOW mirror rule was already logged as pending). `modules/transactions/pay_supplier.php` modal's `#partySelect` dropdown replaced by autocomplete search field (initially built in #26 with select, now upgraded to match):
    - New `modules/transactions/ajax_supplier_search.php` (`?q=`, name/phone/city LIKE WHERE status=1, LIMIT 8, returns id/name/phone/city/current_balance).
    - New `modules/transactions/ajax_supplier_balance.php` (`?id=` returns formatted current balance). NOTE: `purchases/ajax_search.php` + `purchases/ajax_supplier_balance.php` (change #24) remain for the New Purchase page — those are separate endpoints, no conflict.
    - Same `.ac-*` CSS, debounce, `mousedown click`, keyboard nav, click-outside, `ac-empty`, submit-validation (`#supplierError`), `pickSupplier()`.
    - `?supplier_id=` preselect (supplier ledger "Pay" link) still opens modal + fills search box + balance hint; `.pick-party` rows in "To Pay" table still work.
    - Verified (Sep 2026): lint all 3 files; node --check JS passes; search `A` → ALi id 9 (balance 5000); `?supplier_id=9` renders `pickSupplier(9, "ALi")` + modal opens; empty-supplier POST → "Select a supplier" validation. No test rows created (read-only change).

29. **Employee Salary Payments - entry + view + print slip** - client: "employee ki salary kese deni, koi page hi ni bnaya; entry ka page banao, view ka page banao, us page me print bhi lagana ta k employee ko salary slip de saky".
    - **New table `employee_salaries`** (live DB + `database_schema.sql` as table #20): id, slip_no, employee_id FK→employees ON DELETE CASCADE, salary_month VARCHAR(7) YYYY-MM, amount, payment_method cash/bank, bank_account_id FK, payment_date, notes, created_by, created_at. Index `idx_salary_date`.
    - **`modules/employees/salary.php`** (new, admin-only): top-right yellow **"Pay Salary"** button → modal `#salaryModal` (employee autocomplete search `ajax_employee_search.php` + hidden id, salary month `<input type="month">` default current, amount prefilled from employee.salary, payment date, method cash/bank + bank account, notes). On submit: `generateSalaryNo()` (SAL-yymmdd-###) in functions.php, inserts salary row, records cash outflow (`reference_type='salary'`) or bank outflow, activity log. Below: **stat boxes** (Total Slips / Total Paid / This Month / Active Employees) + **All Salary Payments table** (Slip No, Date, Employee, Type badge, Month like "Aug 2026", Method badge, Amount, Print button) + `#paySearch` filter (employee/date/description).
    - **`modules/employees/salary_slip.php?id=`** (new): printable **SALARY SLIP** — Mehboob Traders header, Slip # + date, Employee details (name/type/phone/cnic/address), Salary Month + Payment method, amount table + Net Paid, signature lines (Employee / Authorized), Print button (`window.print()` via `d-print-none`). Mirrors `sales/invoice.php` pattern.
    - **Sidebar** (`includes/header.php`): Employees dropdown + "Pay Salary" item. **Employees list** (`index.php`): Action column + green "Pay Salary" button per row (`salary.php?employee_id=`, opens modal preselected).
    - Verified (Sep 2026): lint clean (5 files); JS node --check OK; render (stat boxes, button, modal, history empty-state); employee search `Mu` → Muhammad Ali id 8 (salary 40000); preselect `?employee_id=8` renders `pickEmployee` + modal opens; E2E POST (temp emp salary 50000 for 2026-08, cash 09-09) → slip SAL-260909-001 + `employee_salaries` row + `cash_book` id 14 outflow + `cash_book_daily` id 6 09-09 + activity log id 42; slip page renders (Slip #, August 2026, Net Paid, signatures); empty-employee POST → "Select an employee". **ALL test rows deleted** (salaries 0, cash_book 10+12 remain, daily only id 3, activity 42 gone).

30. **Salary Ledger (monthly due calculation from joining date)** - client: "aik salary ledger bhi banao jaha show ho agar usko month ki salary ni mili to ab usko kitni milni he, joining date k bad sy calculate hoti rahy".
    - **`modules/employees/salary_ledger.php`** (new, admin-only): calculates for every active employee all months from joining date (`joining_date`, falls back to `created_at` with `*` marker) up to the current month, compares against `employee_salaries.salary_month` records, and shows pending months + total due amount.
    - Layout: info alert + top-right **Print** button (`window.print()`, print-only header with company + "as of" date via `d-none d-print-block`; toolbar/buttons `.d-print-none`). Stat boxes: Active Employees / Employees With Pending / **Total Salary Due (PKR)** (red) / Total Salary Paid. Filter bar: Employee Type dropdown + name/phone search.
    - Main table per employee: Name, Type badge, Joining, Monthly Salary, **Pending Months** badge (red count), **Due Amount** (red `balance-negative`), Last Paid date ("Never" if none), Months button (opens **Monthly Ledger modal** listing every month Paid (green) or Pending—red with amount), + green Pay Salary button (`salary.php?employee_id=`).
    - Logic: `monthRange('Y-m', joining) → today`; pending = due months minus months present in `employee_salaries`; **due amount = pending_months × monthly salary**. Recalculates automatically every month (no manual input).
    - **Sidebar** (`includes/header.php`): Employees dropdown + "Salary Ledger" before "Pay Salary" (active-state checks `salary_ledger` to keep both items distinct).
    - Verified (Sep 2026): lint clean; render (Muhammad Ali joining 09-09 → 1 pending month, PKR 40,000 due, red); E2E (temp loader joining 2026-01-10, salary 30,000 → ledger 9 months pending / PKR 270,000; paid Jan 2026 → 8 pending / 240,000, modal shows Jan = Paid + Feb..Sep Pending); **ALL test data cleaned** (test employee, salary row, cash_book id 15, cash_book_daily id 7, activity log — DB back to client-only rows: employees = only Muhammad Ali).

31. **Salary Ledger: live search bar** - client: "salary ledger me search bar bhi lagao". Replaced the server-side submit button with a **live type-to-filter search input** (`#ledgerSearch`, placeholder "Search name / phone / type...") in the salary_ledger card header — filters `#ledgerTable` rows instantly on `keyup` (name/phone/type match, same pattern as `#paySearch`/`#suppSearch`). Type dropdown (server-side, auto-submit) kept unchanged. Verified: lint clean; render shows input + JS handler; no test data (UI-only change).

32. **Salary payments: Action column (Edit/Delete/Print)** - client: "salary.php page me print column ki jaga action column name rakho... edit, delete or print button". `employees/salary.php`: last column renamed `Print` → `Action`; each row now has **Edit** (outline-warning, opens the same `#salaryModal` in edit mode via `openEditSalary(id)` + `salaryRows` JSON → prefills employee/month/amount/date/method/bank/notes, submit posts hidden `id`), **Delete** (inline form → new `salary_delete.php` with `confirm()`), and **Print** (unchanged `salary_slip.php?id=`). `salary.php` POST handler now branches: if `$_POST['id']` → `update()` the record; else insert new. Both edit & delete reverse the ledger via **new helpers in functions.php**: `removeSalaryLedger()` (deletes linked cash_book row(s), recomputes that day's totals via `recomputeCashDayTotals()` + forwards `recomputeCashDailyFrom()`; deletes linked bank_transactions row and restores `bank_accounts.current_balance`), then edit re-records outflow via `recordCashOutflow`/`recordBankOutflow` (same reference_type='salary', reference_id=salary_id). Modal `show.bs.modal` resets to "Pay Salary" mode unless editing. Verified E2E on temp rows (cash create→edit 15000→12000 updated cash_book+recomputed daily 09-09; delete reversed both cash & bank; bank balance restored) — ALL test rows deleted, DB back to baseline (only client salary id 3 / bank txn id 1 remain). Lint clean all files.

33. **Carton/Box clarity: product edit + clearer 'Boxes per Carton' + purchase entry how-to** - client confused ("samjh ni aa rha, mene boxes add kiye ya carton") about setting how many boxes are in one carton and how to enter cartons/loose boxes on the new purchase page. Fixes:
    - **`product_create.php`**: ambiguous "Boxes / Carton" field renamed to **"Boxes per Carton"** with helper "Boxes in 1 carton (e.g. 48)" + live green preview **"1 Carton = N Boxes"** as you type (`#bpcInput`/`bpcPreview`).
    - **NEW `product_edit.php`** (admin): products.php Action column gets an **Edit** (pencil) button → `product_edit.php?id=` (same fields incl. Boxes per Carton + live preview, Stock labelled "(Boxes)"). This lets the client fix products they already added (their "sooper biscuit" had default `boxes_per_carton=1`). Products list column "Boxes/Carton" → "Boxes per Carton"; **Stock column now shows Boxes + carton equivalent** (e.g. "77 Boxes / 3 Cartons + 5 Boxes").
    - **`purchases/create.php`**: green/blue **"How to enter"** info alert at top of the product section explaining the flow (pick product → carton size auto-fills → type Cartons + Loose Boxes → total auto). Row labels: "Boxes / Carton" → **"Boxes in 1 Carton" (readonly, bg-light, auto-fills from product, note "auto from product")**, Cartons / Loose Boxes / Total Boxes / Rate per Box unchanged, "Carton Total" relabeled **"In Cartons"** ("3 Cartons + 5 Boxes"). `cartonText()` now always returns "X Cartons + Y Boxes"; boor summary `totalBoxes===0` shows "0 Cartons + 0 Boxes".
    - Verified: lint clean (4 files) + node --check JS (purchase & product pages); product edit E2E (temp product bpc 48→24 saved); purchase E2E on temp product (3 cartons×24 + 5 loose = **77 boxes**, stock 77, total 7700, purchase_items.cartons/loose/bpc/qty saved) — **ALL test rows deleted** (product, purchase, items, logs); ALi supplier balance restored to 5000.00. Client's sooper biscuit (id 9) left as-is; edit page now available to correct its bpc.

34. **Purchase total bugfix: Rate is PER CARTON, not per box** - client: "mene 5 cartoon purchase kiye, 2500 price rakhi, 12500 honi chahiye but 250000 show krrha". Root cause: client's product **"Right" (id 11, unit=carton, `boxes_per_carton=20`, price 2500 = per carton)** — the purchase page multiplied total **boxes** (5×20=100) × rate-as-*per-box* (2500) = 250,000. No saved purchase existed (purchases/purchase_items empty), so it was a pricing-semantics bug on the create page, not data. Fixes:
    - **`purchases/create.php`**: `rate` is now **Rate / Carton** (label + help alert updated: "Rate is per Carton … loose box = rate ÷ carton size"). POST math: `per_box = rate_ctn / bpc`; `subtotal = cartons*rate_ctn + loose_boxes*per_box`; stock still box-based (`qty = cartons*bpc + loose`). `purchase_items.purchase_price` stores **effective per-box** (rate_ctn/bpc, rounded) so qty(boxes)×price = subtotal stays consistent on invoices; `products.purchase_price` is updated/last-stored as **per carton** so carton-sized prices survive for display + next prefill. Live JS `recalc()` mirrors the same math + shows a small hint "= PKR x.xx / box" under the rate input.
    - **`sales/index.php`** (same latent bug): product pick now prefills `Rate = sale_price / bpc` (per box), label "Qty (Boxes)"; `sale_price` stays per carton. For bpc=1 products behaviour is identical to before.
    - **`index.php` dashboard**: stock value now `SUM(stock_quantity * purchase_price / NULLIF(boxes_per_carton,0))` (price per carton ÷ carton size → per box), so inventory value is correct for bpc>1 products.
    - No DB migration needed — existing products already follow the per-carton meaning (Right 2500/bpc20, sooper 3200/bpc1).
    - Verified E2E (temp product id 12, bpc 20, price 2500): purchase 5 cartons → **total 12,500**, purchase_items qty 100 @125.00, product price 2500 stock 100; second purchase 1 ctn + 5 loose → 25 boxes, **3,125.00**; sales 20 boxes @140 → **2,800.00** each, stock 125→85. **ALL test rows deleted** (purchases 0, sales 0, cash_book back to 10+12, daily only id 3 [09-08], products 2, suppliers 1, customers 1, logs 57–60 gone). Lint clean (3 files) + node --check both inline scripts.
    - NOTE: client's real purchase **PUR-260909-001** (id 10, ALi supplier) exists → 5 ctn + 1 loose of Right = 101 boxes, total 12,625, @125.00/box, purchased 09-09; Right stock 101, price updated to 2500/carton (fix confirms), ALi balance 12,625. Not touched.

35. **Product list Action column: View / Edit / Delete** - client: "products k list page action column me edit delete or view ka button laga do". `products.php` Action column now has:
    - **View** (outline-info eye icon, ALL roles) → popup modal `#productViewModal` loading `ajax_product_view.php?id=` (admin + salesman/order_booker/loader). Modal shows product details (name/code/status badge, unit, boxes per carton, purchase price / SALE price as "/ carton", min stock, stock in Boxes + Cartons + Boxes, description) then **Recent Purchases** (invoice/date/supplier/cartons/loose/qty/rate-per-box/subtotal) and **Recent Sales** (invoice/date/customer/qty/rate/subtotal) tables — newest 10 each, excludes cancelled.
    - **Edit** (outline-warning, admin) → existing `product_edit.php?id=`.
    - **Delete** (outline-danger trash, admin) → **NEW `product_delete.php`** (POST inline form + `confirm()`): blocks deletion if product appears in `purchase_items` OR `sale_items` (history) — mirrors `supplier_delete.php` safety; else deletes + activity log (module 'product').
    - Verified E2E (temp product 13 w/ 1 purchase + 1 sale): list renders view/edit/delete + modal; view fragment shows name + Recent Purchases (2 ctn +5 loose=25@100) + Recent Sales (5@120); delete blocked with "has purchase/sale history" while history exists → after wiping test history, delete succeeds + log; **ALL test rows deleted** (product 13, purchases 11/12, purchase_items, sale 5, sale_items 3), client data (Right id 11 purchase 10 stock 101) untouched. Lint clean (3 files new/edited) + node --check products.php JS.

36. **Purchase list Action column: View / Edit / Delete / Print** - client: "purchase list page me bhi edit delete or view ka button lagao or print ka button lagao". `purchases/index.php` Action column now has 4 buttons (admin):
    - **View** (eye) → popup modal `#purchaseViewModal` loading **`ajax_purchase_view.php?id=`**: header w/ invoice no + status badge, supplier/date/phone/city/method/paid, items table (product/cartons/BPC/loose/total boxes/rate-per-box/subtotal), Total/Discount/Paid/Due + notes.
    - **Edit** (pencil) → **NEW `purchase_edit.php?id=`** (page fully mirrors create.php, carton/box system):
      - GET pre-populates supplier autocomplete + hidden id, date, method/bank account, each product row (product search + hidden id + bpc + cartons + loose + rate, via embedded `existing_items` JSON cloned into rows), notes/discount/paid.
      - POST reverses old stock (`-quantity`/item), deletes old cash_book outflow + bank_transactions (restores bank balance), deletes old purchase_items, then inserts new purchase/items with per-carton math (rate_ctn → per_box, products.purchase_price = rate/carton), records new cash/bank outflow, **recomputes cash_book_daily** for all affected dates (old + new) via `recomputeCashDayTotals()` + `recomputeCashDailyFrom()`, updates supplier balance (both old & new if changed). Invoice no preserved. Hidden `id` + form `action="purchase_edit.php?id="` (BUG fixed: GET guard reads `$_GET['id']` so the URL must carry it).
      - Rate reconstruction for prefill: `(subtotal - loose×per_box)/cartons`.
    - **Delete** (trash) → **NEW `purchase_delete.php`** (inline POST + `confirm()`): reverses stock, deletes cash_book outflow + recomputes daily totals, deletes bank_transactions + restores bank balance, deletes purchase_items + purchase row, `updateSupplierBalance()`, activity log.
    - **Print** → **NEW `purchase_print.php?id=`** reusable printable invoice (mirrors `sales/invoice.php`): Mehboob Traders header, PURCHASE INVOICE, supplier + payment info, items with cartons/BPC/loose/boxes/rate-per-box, Total/Discount/Paid/Due, `window.print()` + Back button.
    - Verified E2E on temp data (supplier 13, product 14 bpc 10 @1200): create pur 3 ctn=30 boxes @120 → 3,600; list renders all 4 buttons + modal; view shows invoice/product/30; print page shows PURCHASE INVOICE + product; **edit** GET pre-pops, POST 3→5 ctn @1300 → 6,500, stock 30→50, price→1300, cash 3,600→6,500, daily 09-09 correctly recomputed (5,000 client + new); **delete** → purchase+items gone, stock→0, cash_outflow 6,500 removed, daily back to 5,000 only. **ALL test data cleaned** (purchase 13, items, product 14, supplier 13, activity logs), client data untouched (real purchase 10 / Right stock 101 / ALi 12,625 / daily rows 3+10 / cash_book 10+12+22). Lint clean (5 files) + node --check purchase_edit & index JS.
    - (Same session) **Purchase list status column removed** at client request ("status column remove kar do") and the supplier **filter dropdown replaced with autocomplete search-with-suggestions** ("supplier jaha sy select kar rahay hain wahan search bar la ga do") — `purchases/index.php` filter bar now has `#supplierSearch` + hidden `supplier_id`, live-suggestions via `../transactions/ajax_supplier_search.php` (250ms debounce, keyboard nav, click-outside), prepopulates current supplier name from `?supplier_id=`, hint "Filtered by supplier".

37. **Customer & Supplier Ledger: running-balance ledger (opening + transactions + print)** - client: "jab kisi supplier/customer ka ledger open karty hain to first row me opening balance, phir sari transactions (sale/purchase/payment), sath sath balance count hota rahe, end pe current balance, aur print nikal sakain". Rebuilt both `customer_view.php` and `supplier_view.php` as a single chronological running-balance ledger table:
    - **Single table** `#ledgerTable` with columns Date | Description | Debit | Credit | **Balance** (running). Rows merge opening + all transactions sorted by (date, then sub-order opening→sale/purchase→receipt/payment).
    - **Opening row** first (opening_balance, supplier includes `adjustment`). Then each transaction with running balance after every row.
    - **Reconciliation**: sale/purchase row Debit = **due portion** (`total - paid`) so the running balance ties exactly to `current_balance`; receipt/payment row Credit = `amount`. Sign convention same as `updateCustomerBalance`/`updateSupplierBalance` (positive = receivable/payable, negative = advance).
    - **Closing / Current Balance** row in tfoot = `current_balance` (= live balance after `updateCustomerBalance`/`updateSupplierBalance` runs on page load). Colored via `.balance-negative/-positive`.
    - **Print**: print-only company header (Mehboob Traders + CUSTOMER/SUPPLIER LEDGER + party + as-of date) via `d-print-block`, toolbar `.no-print`, two `window.print()` buttons (top + bottom), sidebar/topbar hidden by existing `@media print` CSS. Live search `#ledgerSearch` filters rows.
    - Roles preserved: customer_view = admin/salesman/order_booker (+ area guard), supplier_view = admin.
    - **(After client feedback #2)** customer ledger toolbar polished: **bottom "Print Ledger" button removed entirely**; top toolbar restyled — date filter now a bordered pill group (`bg-light border rounded`, calendar icon, "From/To" labels, inline clear-X) separated by a vertical divider from Receive Payment + Print, on one row. Render + lint verified.
    - Verified: **Ahmad** ledger opening 10,000 → receipt 500 → **9,500** = current_balance ✓; **ALi** opening 5,000 → purchase due 7,625 → **12,625** = current_balance ✓; E2E temp customer (sale 1000 paid 300 due 700 + receipt 400 → closing **300**) reconciles ✓. Print headers + search + buttons render. **ALL test data cleaned** (temp customer 11 + its sale/receipt/items), client data untouched. Lint clean (2 files).

38. **Salesman login removed → only Order Booker has login + salesman-wise invoice assignment** - client: "saleman ka user ni chahiye, bs ordertaker ka user chahiye kio k wo order leta hai, saleman print kar ke deliver karta hai, humy saleman wise invoice chahiye". Workflow now: Order Booker logs in → creates sale (stock reduced immediately, invoice printed) → assigns a Salesman (Deliver By) → salesman takes hard copy, delivers, collects cash → reports back → admin records payment via existing Receive Payment.
39. **Order Booker: customer add access** - client: "order taker ko customer ki access bhi do wo dekh saky add kar saky or sale page ki access bhi do or sale invoice bhi dekh saky". Sale page `/modules/sales/index.php` + invoice list `invoices.php` were ALREADY accessible (requireRole `['admin','order_booker']` since #38) — only customer **add** was blocked. Fixed:
    - **`modules/customers/customers.php`**: POST `add_customer` guard `!isAdmin()` → `!isAdmin() && !isSalesTeam()`; "Add Customer" button, empty-state link, and the Add Customer modal wrapper `isAdmin()` → `isAdmin() || isSalesTeam()`. Edit/Delete buttons stay ADMIN-ONLY. Removed stale hint "Salesmen only see customers from their allocated area" (salesmen have no login now).
    - **`includes/header.php`** sidebar: "Add Customer" link under Customers → `isAdmin() || isSalesTeam()` (order booker sees it; "Pay Amount"/receive_customer stays admin-only).
    - Verified (Sep 2026): lint clean both files; order booker (mali) render → Add Customer button + modal PRESENT, Edit/Delete buttons HIDDEN; New Sale + Invoices HTTP 200 for mali; **E2E temp customer** added as mali (`created_by=6`) → verified → **deleted** (customers + activity_logs cleaned, 0 remaining).
    - **DB**: `sales.salesman_id INT NULL` FK→employees(id) ON DELETE SET NULL (after customer_id, live + `database_schema.sql`).
    - **Salesman login removed**: old `saleman` user (id 6, role salesman) deactivated (status=0) then **converted → order booker**: `username='mali'`, `password='1234'`, role `order_booker`, relinked to employee 8 (Muhammad Ali, employee_type→'order_booker', area Gulberg). Client chose "Muhammad Ali ko order booker bana do". Login today: admin/admin123 + mali/1234.
    - **Employee create.php**: username/password now OPTIONAL for `salesman` type (no `users` row, `employees.user_id` stays NULL) — JS toggles required + hint based on employee_type. Order_booker/loader still require login.
    - **`modules/sales/index.php`**: requireRole → `['admin','order_booker']`; area filter REMOVED (order booker sees all customers); new **"Salesman (Deliver By)"** autocomplete search (`salesmanWrap`/`salesmanSearch`/`salesman_id`), 250ms debounce, mousedown+click, keyboard nav, saves `salesman_id` on INSERT (optional).
    - **NEW `modules/sales/ajax_salesman_search.php`** (`?q=`, employees WHERE employee_type='salesman' AND status=1, LIKE full_name/phone/area, LIMIT 8; roles admin+order_booker).
    - **`modules/sales/invoices.php`**: requireRole → `['admin','order_booker']`; added **Salesman column** (LEFT JOIN employees) + **salesman autocomplete filter** (mirrors purchases supplier filter).
    - **`modules/sales/invoice.php`**: requireRole → `['admin','order_booker']`; printed invoice now shows **"Delivered By: <name> (area)"** under customer; area restriction removed.
    - **Role guards updated** (all `'salesman','order_booker'` → `'order_booker'`): sales/ajax_customer_balance.php, customers/customers.php, customer_view.php, inventory/products.php, inventory/ajax_product_view.php. `isSalesTeam()` → `['order_booker']`. `salesmanArea()` now dead code (no salesman-role users) but left harmless.
    - Verified (Sep 2026): lint clean all 10 files; admin render (salesman search field + hidden id on New Sale; Ajax returns Muhammad Ali when "Mu"); **E2E temp sale** (Ahmad + Apple 2 boxes @15 + salesman 8) → sales.salesman_id=8, invoice.php shows "Delivered By: Muhammad Ali (Gulberg)", invoices list shows Salesman column + filter `?salesman_id=8` → filtered ✓ → **ALL test rows deleted** (sale 7, sale_items 5, stock 13→15 restored, Ahmad balance re-recomputed 9,500). E2E temp salesman employee created WITHOUT login (user_id NULL, shows in salesman search for admin AND order booker) → deleted. Deactivated salesman login → "Invalid username or password". Order booker (mali) dashboard/sidebar: Products (view), Customers (view), Sales (New Sale + Invoices) only — no finance.
40. **New Sale: customer + product `<select>` → autocomplete search (suggestions)** - client: "jo customer me select field lagai he usy remove kar k search field lagao suggestion wali or same kaam product field k sath bhi karo waha bhi apne select field lagai hoi he". Applied the purchases pattern (#24/25) to `modules/sales/index.php`:
    - **NEW `modules/sales/ajax_customer_search.php`** (`?q=`, customers WHERE full_name/phone/customer_no LIKE, ORDER full_name LIMIT 8, returns id/full_name/phone/city/area/current_balance; roles admin+order_booker).
    - **NEW `modules/sales/ajax_product_search.php`** (`?q=`, products WHERE status=1 AND name/code LIKE, LIMIT 8, returns id/code/name/unit/boxes_per_carton/sale_price/stock_quantity; roles admin+order_booker).
    - **Customer field**: old `#customerSelect` dropdown REMOVED → `#customerWrap`/`#customerSearch` autocomplete (250ms debounce, mousedown+click + Enter picks first, ArrowKeys/Enter/Escape nav). Hidden `customer_id` + `#customerError` submit guard ("Please select a customer from the suggestions") + `#customerBalance` hint (fetches ajax_customer_balance.php on pick). Suggestions show name + phone + city.
    - **Product rows**: each row's `.product-select` dropdown REMOVED → per-row `.product-search` autocomplete (mirrors purchases create.php). Hidden `product_id[]` class `.product-id`; on pick sets rate = **sale_price / boxes_per_carton** (per-box, same as old onChange), suggestions show name + code + unit + "In stock: N". Add-row clone resets search input + hidden id. Submit guard: at least one row must have a product picked.
    - Removed server-side `$customers` full-list query (was only for the select); kept `$products` query (still used for POST stock validation).
    - Verified (Sep 2026): lint clean 3 files; ajax returns Ahmad for `?q=ahm`, sooper/Right for `?q=soo`/`?q=right` for BOTH admin and order booker (mali); render shows `#customerSearch` + `.product-search` and NO `customerSelect`/`product-select`; **E2E temp sale as order booker via search-form POST** (Ahmad + Right 2 boxes @140 = 280, paid 0) → sale 8 INV-260910-001 active, stock 101→99, Ahmad 9,500→9,780, invoice.php renders (no salesman → no "Delivered By") → **ALL test data deleted, stock 101 + balance 9,500 restored** (invoice counter is COUNT-based, auto reuses INV-260910-001).
41. **New Sale: Credit default payment method** - client: "inka payment method credit pe he 95% to ap payment method by default credit hi rakhy jab credit ho to amount wali field na show ho". 
    - `modules/sales/index.php`: POST default `$payment_method` 'cash' → **'credit'**; Payment Method select now **Credit (default/selected)** + Cash + Bank with hint "No amount field for credit — full amount goes to due." (Roman-Urdu "Udhaar" removed per item 15). Paid Amount wrapped in `#paidDiv`; JS `#payMethod` change → `#bankDiv` only for bank, `#paidDiv` + hint only when NOT credit, forces `paid_amount=0` on credit; `#payMethod.trigger('change')` on load hides paid field by default. No cash/bank record when credit (existing `paid_amount>0` checks already skip).
    - **DB**: `sales.payment_method` was `ENUM('cash','bank')` → 'credit' would silently store EMPTY string (MySQL non-strict warning). `ALTER TABLE sales MODIFY payment_method ENUM('cash','bank','credit') NOT NULL DEFAULT 'credit'` (live + `database_schema.sql` line 264, comment was already "cash OR credit"). Purchases/cash/bank tables NOT touched (credit is sales-only).
    - Verified (Sep 2026): lint clean; order booker (mali) render → Credit default selected, paidDiv/hint present; **E2E temp credit sale** (Ahmad + Right 3 @140 = 420, payment_method=credit) → rows: payment_method='credit', paid 0, due 420, status active, stock 101→98, Ahmad 9,500→9,920, **0 cash_book rows**; invoice.php prints "Paid: PKR 0.00 (Credit)" + due 420. The first E2E attempt stored EMPTY payment_method (before ALTER) → cleaned; after ALTER re-ran → 'credit' stored correctly. **ALL test data deleted, stock 101 + balance 9,500 restored.**
42. **Order Taker areas + Order-taker-wise invoice list + Salesman-wise Packing List** - client flow: "order taker jata he orders lene shops pe... uska apna area he, sirf apne area k customers dikhen; jab orders le k aye to har order taker ki invoice list chahiye; salesman wise summary invoice/packlist jo hum print de k delivery ke liye; salesman ka apna area he, delivery karke ayega, phir payment system me entry". Implemented:
    - **`includes/functions.php`**: `salesmanArea()` (dead, salesman-role only) RENAMED → **`currentUserArea($pdo)`** — area of the currently logged-in employee from `employees.area WHERE user_id=session` (works for order_booker AND salesman; null if none). No other callers existed.
    - **Area filter — New Sale customer search**: `modules/sales/ajax_customer_search.php` now adds `AND area = ?` (currentUserArea) when NOT admin. Admin always sees all. Verified: mali (Gulberg) sees Ahmad (Gulberg) but NOT a Johar-Town customer; admin sees all.
    - **Area filter — Customers page**: `modules/customers/customers.php` list query adds `AND area = ?` for non-admin (currentUserArea); "Your area: <name>" badge in card header; Add Customer modal `area` input auto-filled + `readonly` for order booker (locked to his area, hint "Area is locked to your area").
    - **Order-taker-wise invoice list**: `modules/sales/invoices.php` — new **Order Taker column** (`LEFT JOIN users u ON s.created_by = u.id` → `order_taker`) + **Order Taker autocomplete filter** (`ajax_order_booker_search.php` new: users WHERE role='order_booker' AND status=1 LIKE full_name/username LIMIT 8; admin-only filter UI). Non-admin order booker is AUTO-filtered to `created_by = session user_id` (sees only his own invoices). Filter form got "Pack List" button when a salesman filter is active.
    - **NEW `modules/sales/packlist.php`** — Salesman Delivery Packing List (print, mirror invoice.php header: Mehboob Traders / DELIVERY PACKING LIST / Salesman+area / period / # invoices / total). Self-contained: salesman autocomplete (ajax_salesman_search.php, readonly-on-pick) + From/To (optional, blank = all) + Load List + Print buttons. Rendered when a salesman is chosen:
      - **Section 1 — Packing Summary (Brand-wise)**: per product brand-wise grouped (`LEFT JOIN brands`, SUM qty/subtotal) with columns Product / Brand / Unit / **Cartons** (`intdiv(qty,bpc)`) / **Loose Boxes** (`qty%bpc`) / Total Boxes / Total Value.
      - **Section 2 — Delivery Detail (Customer-wise)**: per invoice: Invoice # / Customer / Area / Phone / Items (product x qty unit lines) / Total.
      - Excludes cancelled sales. Sidebar Sales menu += "Packing List" link.
    - Verified (Sep 2026): lint clean (packlist.php, invoices.php, header.php, functions.php, ajax_customer_search.php, ajax_order_booker_search.php, customers.php); mali render (Order Taker col present, OB filter hidden for non-admin, packlist HTTP 200) + admin render (Pack List button appears after salesman filter); **E2E**: temp Johar-Town customer (id 13) + temp salesman employee (id 12, Gulberg, user_id NULL) → mali search "TEMP" = [] (area filter works), admin search "TEMP" = found; mali created credit sale (Ahmad + Right 5 @140 = 700, salesman 12, created_by=6) → invoices list "Order Taker=Muhammad Ali", packlist.php?salesman_id=12 → salesman name + Right in Packing Summary + Ahmad in Delivery Detail + total 700; invoices?salesman_id=12 → Pack List button URL right. **ALL test rows deleted** (sale 11, sale_items, stock 96→101, Ahmad 10,200→9,500, temp customer 13, temp salesman 12, activity_logs).
43. **Salary Ledger: modal alignment fix (modals were INSIDE `<tbody>`)** - client: "salary_ledger.php page me right side pe order booker pata ni kia kia show hony lag gya... allignment kharab hogai he". Root cause: each employee row's `#monthsModal*` (Monthly Ledger modal) was rendered INSIDE `<tbody>` (right after `</tr>`) — invalid HTML. Browsers foster-parent those `<div>`s out of the table (they end up shunted after `</table>` / next to tbody), which breaks the table layout and pushes stray markup (employee badges like "Order Booker", modal text) onto the right side / beside rows.
    - **`modules/employees/salary_ledger.php`**: restructured — the row loop now renders only `<tr>`s inside `<tbody>`; the month modals were moved AFTER `</table>` (still inside card-body), rendered by a second `foreach` over the same `$rows` (id/name/salary/month data all still available). Markup now valid: tbody contains only tr, table closes, then modals. HTML output verified: `<tbody>` has 2 `tr` only; `monthsModal13`/`monthsModal8` appear after `</table>`.
    - Type badges in the ledger are correct (Ghufran = `badge-success Salesman`, Muhammad Ali = `badge-info Order Booker`) — the "order booker show hony lag gya" was the broken rendering, not a data error. No DB changes. No test data (HTML-only fix).
    - Verified (Sep 2026): lint clean; admin render Table section clean (2 tr, no divs in tbody), modals after table, page length 472 lines. Serves 2 active employees now: Ghufran (salesman, joining 10-09, salary 50,000, Sep pending → 50,000 due) + Muhammad Ali (order_booker, Sep 2026 paid 20,000).
44. **Salary Ledger: Months detail → paid amounts + remaining balance** - client: "months wale column me button se sari salaries show ho jab joining hui thi, jo pay ho chuki wo show ho, remaining balance show ho". Key fix: partial payments are now respected (previously ANY payment for a month ⇒ month "Paid" fully).
    - `modules/employees/salary_ledger.php`: payments SUMmed per month (`paid_by_emp[emp][month] += amount`); per month `remaining = max(0, salary − paid)`; pending months = months with remaining>0; `pending_amount` = true remaining balance (was `pending_months × salary`). Column "Due Amount" → **"Remaining Balance"**; stat card → **"Total Remaining Salary (PKR)"**. (This version displayed month detail in a modal.)
45. **Salary Ledger Months: modal → separate page with date filter + print** - client: "modal na dikhao, 10 saal purane bande ki sari months modal me dikhana mushkil, alag page banao, date filter + print". Modal on salary_ledger REMOVED entirely (client doesn't want modal for many-months records).
    - **NEW `modules/employees/salary_details.php`** (admin-only): per-employee full month-by-month history. `?employee_id=` + optional **From/To `<input type="month">`** filter (Filter button; blank = all months since joining). Print top-right + print-only company header (EMPLOYEE SALARY DETAILS: name/type/area/joining/Monthly salary/Period/Total Paid/Remaining Balance). Stat boxes: Months Shown / Total Paid / Remaining Balance.
    - Table: **Month | Salary | Paid (with slip no + payment date small text) | Remaining | Status** (Paid / Partial / Pending) + tfoot totals row (Salary total, Total Paid, Remaining Balance). 10-year employees easy: filter by year range, print cleanly.
    - `salary_ledger.php`: Months column button → plain link `salary_details.php?employee_id=` (btn-outline-primary, calendar icon + month count); modal HTML + supporting `details`/`total_paid`/`start_month` row data REMOVED.
    - Verified (Sep 2026): lint clean both files; render — ledger shows 2 links (no modals); emp 8 (Muhammad Ali) page: salary 40,000, Period Sep 2026→Sep 2026, Total Paid 20,000, Remaining 20,000, slip SAL-260909-001 (09-09-2026) under Paid, badge **Partial**, tfoot totals; filter `from=2026-09&to=2026-09` → 1 month only; emp 13 (Ghufran) → Total Paid 0 / Remaining 50,000 / Pending. No PHP warnings; no test data (read-only changes).
    - **Print polish (client feedback)**: print page pe screen-only elements na aayen — "Back to Salary Ledger" button + the whole card-header now carry `d-print-none` (print starts with company header → EMPLOYEE SALARY DETAILS block → table). Verified lint + render.

46. **Customer payment → sales.paid_amount auto-allocation** - client: "sales/invoices.php page me jab me action column sy payment button pe click karta hoo to payment wala page open hota or waha sy me 500 ki entry karta hoo but 'sales/invoices.php' page ka paid wala column update ni ho rha". Root cause: `receive_customer.php` recorded receipts in `customer_receipts` and updated `customers.current_balance`, but **never updated `sales.paid_amount` / `sales.due_amount`**. So invoices always showed paid=0 / due=full.
    - **New helper `allocateReceiptsToInvoices($pdo, $customer_id)`** in `includes/functions.php`: fetches all unpaid/partially-paid sales for the customer (ordered by date ascending = FIFO allocation); for each sale, computes unallocated = `total_amount - paid_amount`; if customer has unallocated receipts sum > 0, allocates min(receipts_remaining, unallocated) to each sale until receipts exhausted. Updates `paid_amount`, recomputes `due_amount = total_amount - paid_amount`, sets `status = 'completed'` when fully paid.
    - Called from `receive_customer.php` after recording the receipt + updating customer balance, inside the same transaction.
    - **DB cleanup**: manually allocated existing 2 receipts (500 each) to sale 12 → `paid_amount=1000`, `due_amount=2300`. Verified E2E: new 500 payment via `receive_customer.php` → `paid_amount` correctly updated to 1500, due to 1800; invoices page shows Paid/Due columns correctly. Test receipt cleaned, DB restored to consistent state.
    - **Also fixed**: `insert()` helper in `functions.php` — keys were being lost (array_key first value captured as string key, rest indexed 0,1,2). Fixed to use `array_values(array_filter(...))` for PDO `execute()`. This was causing silent failures in `recordCashInflow` calls (cash_book entries not being recorded for sales).

47. **Profit bug: cost was computed per-carton × qty-in-boxes (huge negative profit)** - client: "danish customer ki invoice ka total 3300, paid 1050, due 2250, profit PKR -13,950 kese?". Root cause: **unit mismatch in cost calc**. `products.purchase_price` is PER CARTON (e.g. Aata 5kg = 1500/carton, `boxes_per_carton=5`), but `sale_items.quantity` is in BOXES (10 boxes). Formula `purchase_price × quantity` → 1500 × 10 = 15,000 cost, while true cost for 10 boxes = 2 cartons = 3,000. Profit (cash-basis: paid − cost) = 1050 − 15000 = −13,950 (wrong).
    - **Fix**: divide quantity by `boxes_per_carton` when computing cost, in both `invoices.php` (list SQL subquery + stat box) and `invoice.php` (single + per-item + footer): `cost = purchase_price × quantity / GREATEST(bpc,1)`.
    - Verified: sale 12 (10 boxes Aata @330) → cost 3000 → Total Profit **PKR -1,950** (was -13,950) on both list and single invoice pages. Still negative only because customer hasn't fully paid yet (1050 paid of 3300, cost 3000); once paid in full profit = 3300 − 3000 = +300. Lint clean.

48. **Order Booker invoice page = same as admin (Print + Edit/Delete + same columns)** - client: "order booker user me invoice page me print button lgao, action column me edit or delete ka button lagao, same columns hony chahiye jo admin panel me sale invoice k hain, print page laga do admin + order booker dono me". Made `modules/sales/invoices.php` + `invoice.php` + `sale_edit.php` + `sale_delete.php` fully consistent for both roles:
    - **`invoices.php`**: Profit column + `Total Profit` stat box now show for order booker too (admin-only `isAdmin()` wrappers removed). Action column Edit + Delete now render for BOTH roles (Receive Payment stays admin-only — money in/out is admin work). Print icon was already visible to all (target `invoice.php`). Empty-row footer colspan fixed to 10 (was 9).
    - **`invoice.php`** (the print/single invoice page): Profit column in items table + Profit footer row now show for order booker too (`$cs` colspan fixed 6) — printed invoice columns now identical to admin. Print button already `window.print()` visible to all roles. Already role-guarded `['admin','order_booker']` since item 38.
    - **`sale_edit.php` + `sale_delete.php`**: `requireRole(['admin'])` → `['admin','order_booker']`, plus **ownership guard** for non-admin: order booker can only edit/delete invoices where `sales.created_by = session user_id` (their list is already auto-filtered to own invoices). Admin unaffected (all invoices).
    - NOTE: this REVERSES the admin-only Profit hiding from items 52/53 — latest client request wins. Verified: lint clean (4 files); rendered as admin + mali — OB invoices list shows Profit th + Action th + Edit + Delete + Print for own invoices; OB invoice.php?id shows Profit th + >Profit: footer row + print button; admin unchanged (Receive button still there). No test data (read-only/role change; sale 14 = client data untouched).

49. **Invoices list: top-right Print button (print all / per month via filter)** - client: "print button page ke top corner right pe lagao; sab ki invoice ka print chahiye, aur aik mahine ki sale invoices ka print nikalna ho to kese?" `modules/sales/invoices.php` now has a top-right **Print** button (next to New Sale) that prints the CURRENTLY FILTERED register:
    - Filter form From/To does the month selection (e.g. `from=2026-09-01&to=2026-09-30`); leave blank → prints ALL invoices.
    - Print-only header (`d-none d-print-block`, mirrors salary_details pattern): Mehboob Traders / **SALE INVOICES** block with period label ("All invoices" or "01-09-2026 to 30-09-2026"), invoice count, and totals (Sales/Paid/Due/Profit). Screen-only elements hidden on print via `d-print-none` (card-header featuring the button itself, filter form, stat-boxes row — totals already in the print header) + `no-print` on the Action column (th + each row td) so buttons don't appear on paper.
    - Screen view unchanged: Print button top-right corner, filter toolbar, stat boxes, action buttons all still visible on screen. Works for admin AND order booker (same code path).
    - Verified: lint clean; render as admin (Print btn + SALE INVOICES header + no-print th) and as mali with `?from=2026-09-01&to=2026-09-30` (Period: 01-09-2026 to 30-09-2026) + print-only header present. No test data (UI-only change).

50. **Invoice register print → professional layout** - client: "print button laga diya but professional ni he". Upgraded the printed output (screen unchanged) to a proper A4 report in `modules/sales/invoices.php` + `assets/css/style.css`:
    - **Letterhead** (`.report-sheet`, print-only): left = **Mehboob Traders** brand (uppercase bold + "Wholesale Business · Lahore, Pakistan · GST No: --"), right = report title **SALE INVOICES RECORD** + period meta line (e.g. "Filtered invoices: 01-09-2026 to 30-09-2026" + optional salesman/order-taker filter note). Emerald top border (`3px solid #10b981`).
    - **Summary strip** (`.report-summary-table`, print-only): 5 label/value cells — Total Invoices / Total Sales / Total Paid (green) / Total Due (red) / Total Profit (colored by sign).
    - **Data table**: now classed `report-table` — print styles give dark slate header (`#1e293b`) with white uppercase text, 1px grid borders, zebra rows, right-aligned money columns (6–9).
    - **Totals footer** (`tfoot.report-tfoot`): print-only (hidden on screen via CSS) — "TOTAL (N invoices)" + sums for Total/Paid/Due/Profit in an emerald tint.
    - **Footer strip** (`.report-foot`, print-only): Prepared by (<current user full_name>) / Printed on (date-time) / page credit line.
    - **Print CSS block** added to style.css: `@page {size:A4; margin:12mm 10mm}`, hides card border/shadow/header/body padding, table font sizes tuned (header 10px, rows 11px). `style.css` bumped `?v=3` → `?v=4` (stale-cache rule item 25).
    - Verified: lint clean; render admin + mali filtered (report-sheet/title/summary/TOTAL row/Prepared by/Printed on/v=4 all present; meta = "Filtered invoices: 01-09-2026 to 30-09-2026"). No test data (UI-only change).

51. **Customer page for Order Booker: Print + Edit/Delete/View + area sealing** - client: "view customer page jo order booker ka he wo bhi theek karo, page ka print lagao, action column me edit delete view buttons bhi laga do". Applied the invoices-report treatment to the customer list + hardened area rules:
    - **`customers.php`**: top-right **Print** button (screen header) → prints the current list as a professional A4 register (letterhead "Mehboob Traders / CUSTOMERS RECORD" + period/search meta, 3-cell summary strip Total Customers/Receivable/Advance, dark-slate header table w/ zebra + right-aligned balance, print-only tfoot totals row, Prepared by/Printed on footer). Screen-only elements (`d-print-none`): stat-card row, search form, (the print button itself is in the header). Action column th/td get `no-print`. **Edit + Delete buttons now render for order booker too** (was admin-only since item 17/39) — View/Ledger (book → customer_view.php) already visible to all.
    - **`customer_edit.php`**: `requireRole(['admin'])` → `['admin','order_booker']` + **area guard** for non-admin (block if customer.area ≠ currentUserArea). Area input `readonly` + "Area is locked to your area (X)" hint for non-admin; stale "Salesmen only see..." hint fixed.
    - **`customer_delete.php`**: `['admin','order_booker']` + same area guard (defense-in-depth; list is already area-filtered).
    - **`customer_view.php`** (ledger): added non-admin **area guard** (View only own-area customers) + **Edit button** in header toolbar (all roles; Receive Payment stays admin-only). Print already present (item 18/37).
    - Verified: lint clean (4 files); mali (Gulberg) customer list renders 2 own-area customers w/ Edit+Delete+View buttons + Print + report header + Your area badge; **own-area** customer_edit→200 ("Edit Customer" + area-locked hint), **cross-area** (id 15 Johar Town) edit/delete/view → all 302-blocked, customer 15 untouched; ledger (id 8) renders with Edit button + print for mali. Admin page unchanged (7 customers all buttons). No test data changes.

52. **Packing List: merge brand-wise + customer-wise into ONE detail table + print** - client: "2 dafa tables show ho rhy hain brand wise customer wise... merge kar k aik table banao jo shi detail show kary or uska hum print bhi nikaal saky". `modules/sales/packlist.php` now renders a single merged printable table:
    - Query (`$sql3`) enriched with `brand_name` (LEFT JOIN brands) + `boxes_per_carton`; the old brand-summary GROUP BY query (`$summary`) was **removed** (dead code).
    - **One** `report-table`: `# | Invoice | Customer | Area | Phone | Product (Brand) | Cartons | Loose Boxes | Total Boxes | Amount`. Customer/Area/Phone print only on the first row of each invoice (continuation rows blank). Cartons = intdiv(qty, boxes_per_carton), Loose = qty % bpc, Total Boxes = qty, Amount = line subtotal. Print-only tfoot: TOTAL (n invoices) + grand total boxes + grand total amount.
    - Print button (top-right, in toolbar, existing) uses `.report-*` print CSS + `Thead` repeat via `@page`; screen filters/notes are `d-print-none`; "Salesman ka area" hint fixed to English ("Salesman area = delivery route").
    - **Print money alignment refactor** (applies to all report tables): `@media print` nth-child rules replaced with class-based `.report-table th.text-right, .report-table td.text-right { text-align:right !important }`. `text-right` added to: invoices.php thead Total/Paid/Due/Profit + tbody Paid/Due/Profit + tfoot 4 totals; customers.php Balance th + balance td (Total header/cell intentionally left, matches client layout). `style.css` bumped `?v=4` → `?v=5` in `includes/header.php`.
    - Verified: php -l clean (3 files); admin render salesman=17 → exactly ONE `<table>`, old "Packing Summary (Brand-wise)"/"Delivery Detail (Customer-wise)" headings = 0, new heading + tfoot + all carton columns present, INV-260911-001 (2 line items, brand supreme) + INV-260910-001 (brand golden) render, `style.css?v=5` served. `requireRole(['admin','order_booker'])` unchanged.

53. **Packing List print: professional report layout** - client: "packing list page ka print theek karo acha sa banao". Upgraded packlist.php to match the established invoices/customers professional print pattern:
    - **Print-only report header** (`report-sheet d-none d-print-block`): `report-head` with brand line (Mehboob Traders / Wholesale Business · Lahore · GST No: --) and title-box ("Delivery Packing List" + meta: salesman name · period · area); `report-summary-table` with 5 `rs-cell` stat boxes: Salesman, Area, Total Invoices, Total Boxes, Total Amount.
    - **On-screen summary row** (`d-print-none`): Salesman (area), Invoices, Total Boxes, Total Amount — replaces the old always-visible div with border-top (removed) and compensates for `tfoot.report-tfoot` which is hidden on screen.
    - **Print footer** (`report-foot d-none d-print-block`): Prepared by (logged-in user full_name via `$_SESSION`) + Printed on (d-m-Y H:i) + "Mehboob Traders · Delivery Packing List".
    - **Top-right Print button** moved into `card-header` (`d-print-none`, matches client's top-right action button layout); in-form Print button removed. `card-header` now has `d-print-none` so it disappears during print (matches invoices.php).
    - Verified: php -l clean; admin render (salesman=17) → all 13 professional print elements present (report-sheet, report-head, brand-line, summary-table, 5×rs-cell/rs-label/rs-val, report-foot, Prepared by, Printed on, d-print-none×4 on screen elements); old border-top div gone; Print button count = 1 (card-header only). Screen shows filter + summary row + table; print shows letterhead + summary strip + table + tfoot + footer. Cache-bust unchanged (`style.css?v=5`).

54. **Packing List print: readable font sizes + A4 landscape** - client: "print page me bohot choty words dikhty hain iska kia hal he A4 page me nikalna". Added packlist-specific `@media print` overrides (inline `<style>` block before footer):
    - `@page { size: A4 landscape; margin: 10mm; }` — wider layout for 10-column packing table (portrait was too tight).
    - Font sizes bumped: brand-name 24px, brand-sub 11px, title 16px, meta 11px, `rs-label` 10px (with 0.6px letter-spacing), `rs-val` 16px, `report-table th` 11.5px (from 10px), `report-table td` 12.5px (from 11px), tfoot 12.5px, report-foot 10px. All `!important` to override the global print rules in `style.css`. Global invoice/customer reports unaffected (their smaller sizes stay).
    - Verified: php -l clean; render (salesman=17) → landscape + all 8 font-size override rules present in output; summary stat labels/values appear with correct data (Bilal Hussain, Iqbal Town, 2 invoices, 14 boxes, PKR 3,400.00). No test data changes.

55. **New page: Customer Sales Summary sheet (customer invoice-wise)** - client: "aik summary sheet chahiye customer invoice wise". New `modules/sales/customer_summary.php` (requireRole admin+order_booker) + link from invoices.php.
    - **Layout** (established report pattern): print-only `report-sheet` (letterhead + "Customer Sales Summary" title + period/salesman/order-taker meta) with 5-cell summary strip (Customers, Total Invoices, Total Sales, Total Paid, Total Due); on-screen `d-print-none` filter form (date from/to + admin-only order-taker autocomplete + salesman autocomplete) + totals row + optional Print button; card-header top-right **Print** + **Invoices** buttons.
    - **Table**: grouped by customer (keyed by `customer_id`, walk-in → "Walk-in Customer"). `# | Customer | Area | Phone | Invoice | Date | Total | Paid | Due` with **rowspan** merging Customer/Area/Phone across that customer's rows, per-customer **Subtotal (n invoices)** row (`report-group-total`, light-grey bold in print), final `report-tfoot` grand totals, `report-foot` Prepared by/Printed on. Cancelled sales excluded. Same role/guard logic as invoices.php (order booker sees only their `created_by` sales; admin can filter by order taker + salesman).
    - **Print readability**: local `@media print` overrides — th 11.5px, td/group-total/tfoot 12.5px, `rs-label` 10px + 0.6px tracking, `rs-val` 16px (readable on A4 portrait).
    - **invoices.php**: card-header gains **Summary** btn (btn-outline-success) linking to customer_summary.php while preserving from/to/salesman_id/order_booker_id filters (new `$sum_qs`).
    - Verified: php -l clean (2 files); admin render → groups Ahmad/Gulberg (INV-260911-001) + Danish Store/Iqbal Town (INV-260910-001) each with rowspan=2 + Subtotal row, rs-cells populated, tfoot + report-foot present; mali (order booker) render → no order-taker filter + only own invoice (INV-260911-001); invoices.php link preserves `?from=2026-09-01&to=2026-09-30`. DB sales 12+14 unchanged (real client data, no test rows added).

56. **Customer Summary added to Sales sidebar** - client: "customer summary show ni ho rhi sidebar me". Added `<a class="collapse-item" href=".../modules/sales/customer_summary.php">` **Customer Summary** between "Invoices" and "Packing List" in the Sales dropdown (`includes/header.php`). Verified: php -l clean + index.php render shows the link (admin). No CSS/cache-bust change needed (PHP render only).

57. **Customer Summary table fix: phantom empty end-column** - client: "ye end waly column empty he first row or 3rd row ye kese bnaya he table isko theek kar k banao professional way me". Diagnosed + fixed a table layout bug in `customer_summary.php`:
    - **Root cause**: the per-group **Subtotal** row used `colspan="6"` for its "Subtotal (n invoices)" label, but the `#`/Customer/Area/Phone columns above it are `rowspan`-merged into that row. The label then started at column 5 (after the rowspans) and consumed 6 columns → a **phantom 10th column** pushed the subtotal money cells 180px past the data rows, leaving the trailing band **empty on every non-subtotal row** (the data rows, i.e. rows 1 & 3). Confirmed with a headless-Edge layout probe (`getBoundingClientRect`): bad variant rows ended at x=567 (data) vs x=747 (subtotal); fixed variant aligned all rows at x=733.
    - **Fix**: label colspan `6` → `2` (it now covers just the Invoice + Date columns left after the rowspans; the 3 money cells follow in Total/Paid/Due). Re-probed with exact page markup incl. a 2-invoice group (rowspan=3): header/data/continuation/subtotal rows ALL end at x=733 → zero phantom column.
    - tfoot `colspan="6"` LEFT as-is (footer row has no rowspans → 6 + 3 = full 9 columns). Lint clean; live render shows 2× `colspan="2" ...Subtotal` and no other stray colspans. Probe file created + deleted. Rowspan/colspan lesson for future report tables (AGENTS: subtotal inside a rowspanned group must colspan = remaining columns, not full header width).

58. **New page: Order Booker Sales Summary + Profit Margin** - client: "order booker wise bhi aik summary page chahiye or us me profit margin bhi chahiye". New `modules/sales/order_booker_summary.php` (requireRole admin+order_booker) + sidebar link "Bookers Summary" (user-tie icon, below Customer Summary).
    - **Grouping**: sales grouped by order booker (`sales.created_by` → `users.full_name`), keyed by `ob_id`. Guard same as invoices.php — admin sees all bookers, order booker sees only own (`created_by = me`). Filters: date from/to + salesman autocomplete.
    - **Profit**: cash-basis per invoice `profit = paid_amount − total_cost` where `total_cost = SUM(purchase_price × qty / GREATEST(boxes_per_carton,1))` (identical formula to invoices.php, item 56). **Margin %** = profit ÷ total_amount × 100, per invoice, per booker subtotal, and grand total — green (≥0) / red (<0).
    - **Table** (10 cols, landscape A4 print): `# | Order Booker | Invoice | Date | Customer | Total | Paid | Due | Profit | Margin` — Order Booker/# rowspan-merged (rowspan=n+1 incl. subtotal row), booker subtotal row with **`colspan="3"`** label (Invoice+Date+Customer — spans exactly the columns after the rowspans, per item 57 phantom-column lesson) + 5 money cells, tfoot `colspan="5"` + 5 = full width. Screen `d-print-none`: filter form, totals row (Sales/Paid/Due/Profit+margin), card-header Print + Invoices buttons. Print header `report-sheet` + 5-cell rs strip (Bookers, Invoices, Total Sales, Total Profit, Profit Margin) + readable fonts (th 11.5 / td 12.5 / rs-val 16).
    - Verified: php -l clean (2 files); admin render → 2 booker groups (INV-260910-001 admin-created + INV-260911-001 mali-created), rowspan=4 total, Subtotal rows colspan=3, margins render (e.g. -59.1% / -90.9% / grand -60.1% — negative because cash-basis profit; consistent with invoices.php), landscape + report-foot present, sidebar link present; mali render → 1 invoice + 1 booker group (own only). No test data added.

59. **Bookers Summary: merge Profit + Margin into ONE column** - client: "profit margin aik hi column kaha tha mene apne profit or margin alag alag bana diye hain profit wala aik hi rakho column bss". In `order_booker_summary.php` removed the separate Margin column and rs "Profit Margin" box; profit now shows `PKR x (y%)` inline in a single **Profit** cell:
    - thead now 9 cols (`# | Order Booker | Invoice | Date | Customer | Total | Paid | Due | Profit`); invoice rows, booker Subtotal rows and tfoot Profit cells all render `PKR <?=formatCurrency(profit)?> (<?=number_format(margin,1)?>%)` with green/red by sign.
    - Column math (no phantom per item 57): tbody 2 rowspan + Subtotal colspan=3 + 4 money cells = 9; tfoot colspan=5 + 4 = 9.
    - rs strip merged to "Total Profit (Margin)" single box; on-screen Total Profit line already inline `PKR x (y%)`.
    - Verified: php -l clean; render tbody shows `PKR -1,950.00 (-59.1%)` per invoice + subtotal (single cells), thead no Margin th, only "Margin" text left is the rs box label. Margin PHP helpers kept (still used for inline %).

60. **Bookers Summary: remove margin % entirely** - client: "profit k sath jo percentage nikali hoi he usy remove kar do margin ni chahiye". Removed ALL margin display + code from `order_booker_summary.php`: profit cells/subtotals/tfoot now show only `PKR x`; rs box renamed `Total Profit (Margin)` → `Total Profit`; on-screen Total Profit line drops `(y%)`. Dead margin computations deleted (`$margin_total`, `$g_margin`, `$r['margin']`) and `@media print` margin-left:auto kept (CSS). Also fixed my own editing slip that briefly dropped the `require_once header.php` line (caught + restored before verification). Verified: php -l clean; render → profit cells `PKR -1,950.00` (no %), rs box `Total Profit`, only `%` remaining is the JS modulo operator. Profit column count/colspan unchanged (9 cols).

61. **Employees page: live search bar** - client: "employess page me search bar lagana". `modules/employees/index.php` card-header now has a search input (`#empSearch`) before the Type dropdown. Client-side JS filter (no page reload): on `input`, it toggles `#employeesTable tbody` rows (`:not(.empty-state)`) matching the query against the row's text (name/type badge/area/phone/CNIC), shows a `#empNoMatch` "No employees match your search." row when zero matches. Existing empty-state row got `.empty-state` class to keep it out of the toggle. Works alongside the existing admin-only Type (server-side) dropdown. Verified: php -l clean; render shows search input + table id + JS + no-match row, employee rows render normally.

62. **Employees page: fix search bar alignment** - client: "search bar to laga di iski allignment to theek karo". Restructured the card-header right side: search input + Type dropdown + Add Employee button are now all INSIDE one `<form class="form-inline mb-0">` (single flex row, `align-items:center`), card-header is `d-flex align-items-center justify-content-between flex-wrap`. Before, the button sat in a separate `.d-flex flex-wrap` div next to the form-inline and dropped onto its own line / misaligned. Added `onkeydown="if(event.key==='Enter')event.preventDefault();"` on the search input so Enter won't submit/reload (live filter stays). Note: tried verifying with headless-Edge `getBoundingClientRect()` on a throwaway probe page but CDN bootstrap load timing made the headless measurements garbage (even showed full-width 740px blocks) — discarded probe, relied on the standard single-form-inline pattern instead. Verified: php -l clean; rendered page has exactly one `form-inline mb-0` containing `#empSearch`, the Type select, and the Add button.

63. **Add Print buttons to all list/view pages** - client: "ab ap ne un pages me print button lagana he jaha zroorat he jese purchase list page, product list, cash book, bank book, view supplier, pay amount, view customer, receive customer, view employees, salary ledger, salesman wise packing list, expense". Added `onclick="window.print()"` + professional `d-none d-print-block` letterhead blocks (Mehboob Traders + page title + filter meta + Printed on date) to the **8 pages that were missing them**: `modules/purchases/index.php`, `modules/inventory/products.php`, `modules/cashbook/index.php`, `modules/bankbook/index.php`, `modules/transactions/pay_supplier.php`, `modules/transactions/receive_customer.php`, `modules/employees/index.php`, `modules/expenses/index.php`. Each page: Print button added at top-right (next to existing action buttons, wrapped in `d-print-none`); filter/control rows (`<form>` or entire top `row mb-3`) marked `d-print-none`; action columns (Action th/td in purchase/product/employee tables + Pay/Receive button th/td in pay_supplier/receive_customer first tables) marked `d-print-none`; search inputs in pay_supplier/receive_customer second card-headers marked `d-print-none`; Add-Expense form card (expenses col-lg-5) wrapped `d-print-none`. **Also added generic list-print CSS** to `assets/css/style.css` (bumped `?v=5` → `?v=6` in `includes/header.php`): placed BEFORE the existing report-print block so report overrides win; rules: `.badge { color:#0f172a !important; background:transparent !important; border:1px solid #94a3b8 }` (prevents white-on-white badge text), `.table td,.table th { color:#0f172a !important }`, `.table-hover tbody tr:hover td { background:transparent !important }`, `a { color:#1e293b !important }`. **Pages that already had print** (confirmed via grep): `supplier_view.php` (2 Print buttons + letterhead), `customer_view.php` (Print button + letterhead), `salary_ledger.php` (Print button + letterhead), `packlist.php` (Print button + professional report-sheet), plus all report pages (invoices/invoice/customer_summary/order_booker_summary). Verified: php -l clean on all 9 files (8 PHP + header.php); render checks on purchases, pay_supplier, expenses all show `onclick="window.print()"` and correct letterhead text.

64. **CONVERSION → Scrap Management System** - full project copy of mehboob_traders converted to a scrap business: DB `scrap_management_system`, served at `http://localhost/scrap_management_system/`. Existing files edited only (no rewrites, no test data). Summary of all targeted changes:
    - **Carton/Box system removed** (scrap sells per-unit, no cartons): `product_create.php`/`product_edit.php` — deleted `boxes_per_carton` insert/update + bpc input + live preview JS, default unit `'pcs'`→`'kg'` (dropdown slimmed to kg/ton/pcs/gadda/pack/dozen/meter/liter), stock input labelled "(Units)" step=any. `products.php` — "Boxes per Carton" thead column removed, stock shows plain float + unit (no carton math), empty-row colspan 10→9. `ajax_product_view.php` — dropped `pi.cartons/loose_boxes` from SELECT, deleted `$bpc/$stock_ctns/$stock_rem` math, prices per unit, purchase/sale tables simplified to Qty (Units)/Rate/Subtotal, no stock-prefill logic.
    - **Suppliers**: `suppliers.php` insert array + add-modal + table gained **Plant Area** + **Plant Name** columns (after City, colspan 8→10); `supplier_view.php` info card + supplier line show plant info; `supplier_edit.php` form + POST include both fields.
    - **Customers**: `customers.php`/`customer_edit.php`/`customer_view.php`/`customer_delete.php` — **all `area` handling removed** (`customers.area` doesn't exist in this DB; order-booker area-guard + `$my_area` filter + Area column/badge deleted) and replaced with **Plant Area + Plant Name** (add/edit forms, info card, list columns; empty-row colspan 8→9, footer span 6→8). `customer_receipt.php` verified unchanged.
    - **Transactions**: `ajax_customer_search.php`/`ajax_supplier_search.php` SELECT now return `plant_area, plant_name`; `pay_supplier.php` + `receive_customer.php` autocomplete suggestions show Plant Area + Plant Name, the "with Balance" tables add a Plant column (colspan 5→6), print headers branded.
    - **Branding** (ALL printable headers, stat cards, letterheads, footers, sidebar already runtime-based): "Mehboob Traders" → **"Scrap Management System"**, "Wholesale Business"/"Wholesale" → **"Scrap Business"** on employees (index/salary_ledger/salary_details/salary_slip), expenses/index, cashbook/index, bankbook/index. `assets/js/main.js` comment header re-themed. `expenses/categories.php`, `reports/index.php`, `modules/profile/index.php` verified no branding references.
    - **Base URL**: absolute `/mehboob_traders/` → `/scrap_management_system/` fixed in `supplier_view.php`, `customer_view.php` (+ verified others). Out of scope per brief: `config/db.php`, `includes/*`, `login.php`, `index.php`, `modules/purchases/*`, `modules/sales/*` (their carton/box pricing semantics are unchanged by design — purchases/sales still use the existing per-carton math + `boxes_per_carton` only where it exists; note this DB keeps those columns).
    - **Verified**: php -l clean on all 22 edited files; DB schema confirmed (`plant_area`/`plant_name` present on suppliers+customers, no `customers.area`, no `products.boxes_per_carton`); live render 200 for products/create/suppliers/customers list+view+edit/pay_supplier/receive_customer/employees/expenses/cashbook/bankbook (14 pages; supplier view/edit id=1 renders the clean "not found" redirect since suppliers table is empty). Post-conversion grep clean for Mehboob/mehboob_traders/bpc/bpcInput/carton math/customer area in edited scope (residual matches only in PROJECT_NOTES.md history + out-of-scope purchase/sales/includes). PNG-2 letterhead brand-line format (brand + "Scrap Business · Lahore, Pakistan · GST No: --") left for client to confirm.

## ==== SESSION RESUME (10-Sep-2026, updated during session) ====
GOAL of this session: **New Sale page: remove default 0 from input fields** (item 50) — client: "sales/index.php me kuch fields me 0 by default likha ata he, pehle 0 remove karna parta hai, usko remove kar do jaha jaha 0 likha he". Removed `value="0"` from **Qty (Boxes)**, **Discount**, **Paid Amount** inputs (added `placeholder="0"`). JS `recalc()` already uses `parseFloat(...) || 0` so empty = 0 math stays correct; credit method still hides Paid + forces 0. Readonly auto-calculated fields (Subtotal 0.00 / Total / Due) left as-is (no typing needed). Verified lint + render (no `value="0"` left in form inputs).
GOAL of this session: **Profit: cash-basis (paid − cost, negative until paid)** (item 56) — client: "jab tak pese ni diye to minus me profit show ho but jese hi is invoice k against paise de customer to profit ki value update ho". Changed formula from realized-margin to **cash-basis P&L**:
    - **New formula**: `profit = paid_amount − total_cost` where `total_cost = SUM(purchase_price × quantity)`.
    - **Before payment** (credit sale): profit = 0 − cost = **negative** (represents inventory given out).
    - **After payment**: profit = payment − cost (becomes less negative or positive).
    - SQL subquery now computes `total_cost` instead of `raw_margin`.
    - Both `invoices.php` (list + stat box) and `invoice.php` (single + per-item + footer) updated.
    - Footer profit row color now dynamic (green ≥0, red <0).
    - Verified: sale 12 (paid 0, cost 15,000) → profit **-15,000** ✓. When customer pays via `receive_customer.php`, profit auto-updates on next page load.
GOAL of this session: **Profit: realized-only (proportional to payments received)** (item 54) — client: "jab koi payment kary ga to wo profit update hoga?" — yes. Changed both `invoices.php` (list) and `invoice.php` (single) to show **realized profit** instead of raw margin:
    - Formula: `realized_profit = raw_margin × (paid_amount / total_amount)`. Raw margin = `SUM((sale_price − purchase_price) × qty)`.
    - Credit sale with no payment → profit = 0. Partially paid → proportional. Fully paid → full margin.
    - Verified: sale 12 (INV-260910-001, paid 0/3300) → profit PKR 0.00 ✓. When a payment is later received via `receive_customer.php`, `updateCustomerBalance` recomputes and the profit column auto-updates on next page load.
    - Both list page stat box ("Total Profit") and per-row/per-item cells use the same realized formula. Lint clean, renders OK.
GOAL of this session: **Invoices list: add Profit column, remove Method + Status columns** (item 53) — client: "sales/invoices.php is page ki bat karrha hoo yaha column add karna he profit margin status, method column remove kr do". Updated `modules/sales/invoices.php`:
    - **Main SQL** now includes a correlated subquery: `(SELECT COALESCE(SUM((si.price - p.purchase_price) * si.quantity), 0) FROM sale_items si JOIN products p ON si.product_id = p.id WHERE si.sale_id = s.id) AS profit`.
    - **Removed** Method (`badge-secondary`) and Status (`badge-warning`/`badge-success`/`badge-danger`) columns entirely (both thead + tbody cells).
    - **Added** Profit column in thead + tbody (admin-only) — green/red color-coded per sale.
    - **Added** "Total Profit" stat box (admin-only, between Total Due and end) alongside the existing 3 stat boxes (Total Sales/Paid/Due now `col-md-3` each instead of `col-md-4`).
    - **Footer colspan** on empty-row: dynamically 9 (admin) / 8 (order_booker).
    - Verified lint + admin render (Profit column + Total Profit stat visible, no Method/Status) + order_booker render (colspan=8, no Profit/Method/Status). No test data.
GOAL of this session: **Sale invoice: add profit margin** (item 52) — client: "sale invoice me profit margin bhi chahiye". Updated `modules/sales/invoice.php`:
    - SQL query now joins `products.purchase_price`; per-item profit = `(sale_price − purchase_price) × quantity`.
    - **Profit column** in items table (admin-only, both screen + print) — green if positive, red if negative.
    - **Profit total row** in footer (admin-only, between Discount and Paid rows).
    - Footer colspan dynamically 6 (admin) / 5 (order_booker) to match column count.
    - Order bookers see no profit data at all (column + footer row both hidden). Verified lint + admin render (profit column visible) + order_booker render (0 matches for "Profit"). No test data.
GOAL of this session: **Sales invoices: add Edit / Delete / Print action buttons** (item 51) — mirroring the purchases pattern (`modules/purchases/index.php` action column). Changes:
    - **`modules/sales/invoices.php`**: Header last column changed `View → Action`. Each row now shows: **View** (eye, `invoice.php`, all roles), **Edit** (pencil → `sale_edit.php`, admin-only), **Delete** (trash → `sale_delete.php`, admin-only via inline POST form + confirm prompt), **Print** (printer → `invoice.php` in new tab, all roles). All buttons use `text-center nowrap`.
    - **NEW `modules/sales/sale_delete.php`** (admin-only): POST-only delete mirroring `purchase_delete.php` reversal logic — reverse stock (`+qty` per `sale_item`), delete `cash_book` inflow rows for `reference_type='sale'` and recompute affected `cash_book_daily` totals + running balances via `recomputeCashDayTotals`/`recomputeCashDailyFrom`, delete `bank_transactions` rows and restore bank account balances, delete `sale_items` + `sales` row, call `updateCustomerBalance`. Blocked if already cancelled.
    - **NEW `modules/sales/sale_edit.php`** (admin-only): Full edit form mirroring `purchase_edit.php` for sales — customer / salesman (autocomplete search + balance hint), sale date, payment method (credit/cash/bank + bank account), products grid (qty + rate + subtotal), discount, paid, notes. On POST: reverse old stock/cash/bank (delete inflows, restore bank balance), delete old `sale_items`, update `sales` row (preserve `invoice_no`/`created_by`/`created_at`, recomputed status active/completed), insert new `sale_items` + deduct new stock, record new `cash_book`/`bank_transactions` inflow, recompute cash daily, `updateCustomerBalance` for old + new customer. Stock validated against post-reversal stock (`current_stock + old_qty_for_this_product ≥ new_qty`) using helper `old_qty_for()`. Inherits keyboard autocomplete + submit guards from `index.php` JS.
    - **E2E verified**: created temp customer (TST-051-0001) + temp sale (3×Right @100, cash paid 100) → confirmed stock 101→98, cash inflow 100, balance 200; edited to 2×Right @120 fully paid → stock 99, cash 240, balance 0, status completed; deleted → stock 101, sale/items/cash rows gone, daily reconciled; insufficient-stock guard tested (qty=999 blocked correctly → 302 back to edit). All test data cleaned: temp customer deleted, empty `cash_book_daily` row for test date removed. Lint clean on all three files.
GOAL of this session: **Salary payments page: date filter** (item 49) — client: "modules/employees/salary.php page me bhi date filter lagao" (mirrors cash/bank book #23). `salary.php` (All Salary Payments, admin):
    - New **Date Filter Toolbar** (card above table, `d-print-none`): From/To date inputs + **Filter** button + **All** reset + **This Month** preset link + period badge (`01-09-2026 to 30-09-2026`) when filtered.
    - Payments query server-side filtered by `s.payment_date` range (parameterized); **Total Slips** + **Total Paid** stat boxes now reflect the filtered range; **This Month (PKR)** stays overall current-month (separate `SUM` query, unfiltered). Edit/delete/print actions operate on visible (filtered) rows.
    - Verified (Sep 2026): lint clean; render no-params (toolbar + This Month + stats) & filtered `?from=2026-09-01&to=2026-09-30` (period badge + SAL-260909-001 still listed; outside dates would be excluded). No test data (UI + read-only change).
GOAL of this session: **Loader login removed** (item 48) — client: "loader ka bhi user ni chahiye" (same as salesman). Now only Order Booker employees get login accounts.
    - `modules/employees/create.php`: `$needs_login` → order_booker only; hint text (server default + live JS) → "For Order Booker, login is required. For Salesman and Loader, no login is needed."; JS `needsLogin` checker only order_booker.
    - **DB**: loader user accounts deleted (rashid/naveed/asif/tariq/yousaf from `users`), `employees.user_id = NULL` for all loader employees. Users now: admin + 6 order_booker logins (mali/sajid/arslan/zahid/faisal/kamran, pw 1234 except mali).
    - `toggle_status.php`/`delete.php` already guard `!empty($emp['user_id'])` → no crash for login-less employees. Loader role access in auth code stays harmless (no loader users exist).
    - Verified: lint clean; login rashid/1234 → **"Invalid username or password"**; create.php hint renders "No login is needed" for Salesman/Loader.
GOAL of this session: **Test data: 5 salesmen + 5 order bookers + 5 loaders + 6 customers + 6 suppliers** (item 47) — client: "5 saleman add karo, 5 loader add karo, 5 ordertaker add karo, 6 customers add karo, 6 suppliers add kar do". Client requested test entries (kept in DB for their workflow testing):
    - **Employees** (+15): salesmen Rizwan Ahmed (Gulberg), Shahid Mahmood (Model Town), Imran Khan (Samanabad), Bilal Hussain (Iqbal Town), Usman Aslam (Wapda Town) — NO login (user_id NULL). Order bookers Sajid Ali (Johar Town), Arslan Mehmood (Model Town), Zahid Iqbal (Gulberg), Faisal Raza (Samanabad), Kamran Shahid (Iqbal Town). Loaders Abdul Rashid, Naveed Anwar, Asif Mehmood, Tariq Aziz, Mohammad Yousaf. All login users password **1234** (passwords stored PLAINTEXT in `users` — login.php compares `$password === $user['password']`). Usernames: sajid/arslan/zahid/faisal/kamran + rashid/naveed/asif/tariq/yousaf. Employees now: 6 salesman / 6 order_booker / 5 loader.
    - **Customers** (+6, CUS-2609-0002..0007): Karim General Store (Gulberg), Shafi & Sons (Johar Town), Rehman Kiryana (Model Town), Salman Traders (Samanabad), Danish Store (Iqbal Town), Malik Sweets & General (Wapda Town). 0 balance, created_by admin(1).
    - **Suppliers** (+6): Punjab Food Mart, City Traders, Lahore Distributors, Bismillah Suppliers, Madina Enterprises, Global Foods (contact persons + phones, 0 balance).
    - **Verified**: login sajid/1234 + rashid/1234 (302); area filter — sajid (Johar Town) finds Shafi & Sons, NOT Karim (Gulberg, empty []); loader dashboard 200. NOTE: customers.created_at use CURDATE; no `status` column on customers (first insert failed on that — removed).
GOAL of this session: **+8 products for client testing (salesman/order-taker workflow)** (item 46) — client: "mujhe ap 7-8 products add kar k dikhao, mene saleman or ordertaker ki entries kar k check karni he k kese work karrha he". Added (live DB, client asked):
    - Categories **Toffees** (5), **Bubbles & Balloons** (6), **Nimko** (7), **Flour / Aata** (8); Brands **mazaa** (5), **supreme** (6), **golden** (7).
    - Products (all `unit=carton`, per-carton purchase/sale price, stock > 0 so sales can be tested): **Mango Toffee** (24/bpc, 720/840, stock 200), **Lollipop Jar** (12, 360/420, 150), **Bomb Bubbles** (12, 300/360, 120), **Balloon Pack (100)** (10, 250/300, 90), **Nimko Mix** (24, 480/600, 60), **Saltish Peanuts** (12, 240/300, 80), **Aata 5kg** (5, 1500/1650, 40), **Chocolate Biscuit** (24, 1800/2000, 100). Ids 25–32. (First insert attempt failed: MariaDB `||` = logical OR, so codes collided → took `CONCAT('PRD-',DATE_FORMAT(NOW(),'%y%m%d%H%i%s'),'NN')` — burned auto-increment ids 17–24, harmless, no stray rows.)
    - **Verify (order booker mali)**: product search "toffee" → Mango Toffee (carton 840, stock 200), "nimko" → Nimko Mix ✓; salesman search "Gh" → Ghufran (Johar town) ✓; New Sale page HTTP 200. Client can now: create sale as mali → assign Ghufran (Deliver By) → check invoices + packlist.php (salesman-wise) + inventory stock reduce.
GOAL of this session: **Salary Ledger Months button → separate detail page + date filter + print** (item 45) — client "modal na dikhao (10 saal wale bande ki sari months modal me kahan dikhain), alag page banao, waha date filter + print". Removed the Months modal from salary_ledger.php and made the column button link to NEW `salary_details.php?employee_id=` (admin-only): month-by-month table (Month|Salary|Paid+slip+date|Remaining|Status badge Paid/Partial/Pending), From/To `<input type="month">` filter + Filter button, top-right Print + print-only header, stat boxes (Months Shown/Total Paid/Remaining Balance), tfoot totals. Verified lint + renders (emp 8: paid 20k/slip; partial; filter 1 month; emp 13 pending 50k). No test data.
GOAL of this session: **Salary Ledger Months button → full month-by-month detail incl. paid amounts + remaining balance** (item 44) — client "months wale column me button se sari salaries show ho jab joining hui thi, jo pay ho chuki wo show ho, remaining balance show ho". Before this the Months modal only marked a month Paid/Pending (any payment = fully paid → Muhammad Ali's 20,000 on 40,000 salary looked "fully paid"). Rewrote:
    - **`modules/employees/salary_ledger.php`**: payments now summed **per month** (`paid_by_emp[emp][month] += amount`); per employee each due month gets `details[salary, paid, remaining = salary − paid, >=0]`; pending months = months with remaining>0; `pending_amount` = true remaining balance (sum of per-month remaining, replacing `pending_months × salary`); new `total_paid` per employee. Partial payments (0<paid<salary) are preserved.
    - **Months button modal** now: summary line (Salary/month, From/To month count, **Total Paid** green, **Remaining Balance** red) + per-month table columns **Month | Salary | Paid | Remaining | Status** with badges Paid (paid≥salary) / **Partial** (0<paid<salary, warning) / Pending (none). "Due Amount" column renamed **"Remaining Balance"**; stat card renamed **"Total Remaining Salary (PKR)"**; alert text updated.
    - Verified (Sep 2026): lint clean; render — Ghufran (no payments): remaining 50,000, Pending badge; Muhammad Ali (Sep paid 20,000 of 40,000): Total Paid 20,000, Remaining **20,000**, status **Partial** (previously wrongly shown as fully Paid). Total Remaining box = 70,000. No test data (read-only page change).
GOAL of this session: **Salary Ledger alignment fix** (item 43) — client "salary_ledger.php page me right side pe order booker pata ni kia kia show hony lag gya, allignment kharab hogai he". Fixed: month-per-employee modals were inside `<tbody>` (invalid HTML → browser foster-parenting shunted modal/row-markup to the table's right, scrambling "Order Booker" etc). Moved all `<#monthsModal*>` blocks AFTER `</table>` via a second `$rows` foreach; tbody now contains only `<tr>`. Verified lint + admin render (clean tbody, modals after table). No test data (HTML-only fix).
GOAL of this session: **New Sale search fields** (item 40) — client "customer select remove kar k search field suggestion wali, same product field". Converted both to autocomplete (purchases pattern): customer via new `ajax_customer_search.php` + balance hint + submit guard; products per-row via new `ajax_product_search.php` (rate auto = sale_price/bpc). E2E temp sale as order booker verified then fully cleaned. (Earlier in session: item 39 customer-add access for order booker — also verified + cleaned.)
GOAL of this session (cont.): **Credit default on New Sale** (item 41) — client "payment method 95% credit par hai, default credit rakho, credit ho to amount wala field na show ho". Added Credit option (default selected), paid field hidden on credit, `sales.payment_method` ENUM extended to include 'credit' (live ALTER + schema mirror). E2E credit sale verified (payment_method=credit, due=total, no cash row) then fully cleaned.
GOAL of this session (cont.): **Areas + order-taker list + salesman pack list** (item 42) — client flow: order taker ka apna area, sirf apne area k customers; har order taker ki invoice list; salesman-wise brand-wise packing list print. Added `currentUserArea()` (renamed dead salesmanArea), area filter on New Sale customer search + Customers page (order booker locked to his area, admin sees all), Order Taker column + filter on invoices (non-admin auto sees only his own), and NEW `packlist.php` (salesman-wise, brand-wise packing summary + customer-wise delivery detail, printable). E2E: temp cross-area customer + temp salesman + credit sale → packlist verified → ALL cleaned.

## ==== SESSION RESUME (09-Sep-2026, updated during session) ====
GOAL of this session: **Order Booker customer access** (item 39) — client "order taker ko customer ki access do, wo dekh saky add kar saky or sale page ki access bhi do or sale invoice bhi dekh saky". Sale page + invoice pages were already open to order_booker (since #38). Enabled customer ADD for order booker: customers.php POST guard + Add button + modal + sidebar Add Customer link (`isAdmin() || isSalesTeam()`), edit/delete stay admin-only. E2E: mali added temp customer (created_by=6) → deleted + logs cleaned. Lint clean.

## ==== SESSION RESUME (09-Sep-2026, updated during session) ====
GOAL of this session: **Fix wrong purchase total (invoice shows 250,000 instead of 12,500)**. Diagnosed: no saved purchase existed — it was live calc on New Purchase page. Client's "Right" product (unit=carton, bpc=20) priced **per carton** (2500) but the page charged **per box** → 100 boxes × 2500 = 250,000. Fixed #34 (Rate/Carton semantics on purchase + sales prefill + stock value), E2E-verified (5 cartons → 12,500; loose boxes → 3,125; sale 20 boxes @140 → 2,800) and ALL test data cleaned. NOTE: client's real products now are "sooper biscuit" (id 9, bpc 1, 3200/3500) and "Right" (id 11, bpc 20, 2500/2800) — both prices are **per carton** going forward.
GOAL of this session (cont.): Product list (item 35) + Purchase list (item 36) Action columns. Products: View (modal) / Edit / Delete buttons all added. Purchases: View (modal) / Edit / Delete / Print added, with full stock + cash/bank reversal on edit & delete. Both E2E-verified on temp data then ALL test rows cleaned (client data: purchase 10 Right 101 boxes @125 = 12,625, ALi bal 12,625, daily rows 09-08 & 09-09, cash_book 10/12/22 — all untouched). Purchase list supplier filter → autocomplete search; status column removed.
GOAL of this session (cont.): Customer & Supplier ledgers rebuilt as running-balance ledgers (item 37): single chronological table w/ opening row first, every sale/purchase/payment/receipt showing Debit/Credit and a live running Balance that ties to `current_balance`, closing/current-balance row at bottom, print-only company header + Print button + live search. Verified reconcile: Ahmad 10,000→9,500; ALi 5,000→12,625; E2E partial-payment sale (700 due + 400 paid) → 300. ALL test data cleaned.
GOAL: items 29-30 (salary module). Item 29: employee salary — Pay Salary entry (modal + autocomplete employee search, cash/bank outflow, SAL- slip no) + All Salary Payments view (stat boxes, search) + printable Salary Slip page. Item 30: Salary Ledger — computes per-employee pending months since joining date and total due automatically, month-by-month Paid/Pending modal + print. Both E2E-verified with temp employees then ALL test data cleaned (DB back to client-only: 1 employee, 1 customer, cash book rows 10+12, daily row 3). New DB table: `employee_salaries` (live + schema table #20).
GOAL of this session: New Purchase page — supplier select → search bar with DB suggestions; product field → search bar with suggestions. Items 24-25 DONE + tested. E2E full purchase verified then wiped; stale-page cache bug diagnosed via headless-Chromium probe and fixed with no-store headers + CSS/JS version bumps. Item 26: Pay Supplier page rebuilt mirroring Receive Customer (modal + history + search), E2E cash payment verified then wiped. Items 27-28: Receive Customer + Pay Supplier modals upgraded from `<select>` to autocomplete search-with-suggestions (+ balance hint via new ajax endpoints); both verified read-only (no test rows).

### WORKFLOW (koi bhi next session aise chalte rehna)
1. PADHNA: `PROJECT_NOTES.md` sab se pehle is file ko read karo (yehi persistent memory hai).
2. CLIENT KI HAR REQUEST: Roman-Urdu me aati hai. Sab UI text ENGLISH me likho (item 15 - client ne "urdu use ghalat" kaha).
3. LAYOUT RULES (client zyada tar yahi kehta hai): peer-centered UI, top-right corner button, popup modal se forms, page par sari entries ki table, search bar for filtering, print button, no separate views - sections ek hi page par.
4. TESTING HARR RENDER YA LOGIC KE BAAD:
   - Lint: `& "C:\xampp\php\php.exe" -l <file>`
   - Render (login cookie): login `curl.exe -s -c $jar -d "username=admin&password=admin123" -X POST http://localhost/mehboob_traders/login.php -o NUL` then `curl.exe -s -b $jar http://localhost/mehboob_traders/<page> -o <tmp>` and grep output.
   - DB checks: `& "C:\xampp\mysql\bin\mysql.exe" -u root mehboob_traders -e "SQL"`.
   - End-to-end: test-data banao → verify → WAPAS CLEAN KARO (client ka data kabhi nahi chodna; history-check benchmark is 'delete karna test-clean').
5. CLIENT DATA SAFETY: `customer_delete.php` / `supplier_delete.php` block delete sirf txn HISTORY par (sales/receipts/purchases/payments). Opening-balance-only records protect NHI hote — careful. (Incident hua: "Ahmad"/"ALi" ghalti se delete + restore, item 17.)
6. NO COMMIT/PUSH unless client explicitly asks.
7. HARR DONE CHEEZ: PROJECT_NOTES.md me short item append karo (#24, #25...).
8. Billable modules bache hain? Pending client to decide (item 11 / reports done already). Naye requests: client will tell.
9. MIRROR RULE: jab bhi customer-side ka koi change ho, supplier-side match karo (aur vice versa). DONE-hot hai — create.php, receive_customer.php (#27) aur pay_supplier.php (#28) sab me autocomplete search+balance hint hai. Naye payment/receive pages aayein to yahi pattern use karo.

### ENV FACTS
- Path: `C:\xampp\htdocs\mehboob_traders` | PHP: `C:\xampp\php\php.exe` | MySQL: `C:\xampp\mysql\bin\mysql.exe` (root, no pw, db `mehboob_traders`)
- Login: admin/admin123 | order booker: mali/1234 (Muhammad Ali, ex-salesman; salesman-type employees now have NO login)
- DB tables: cash_book_daily (per-date opening/closing), bank_accounts (opening_balance, opening_date, current_balance), customer_receipts, supplier_payments, employee_salaries (salary slip #29).

64. **All Expenses page: full-width list + top-right New Expense popup modal** - client: "all expense page open kary to poory page expense list show honi chahiye, new expense ka button page k top right side pe ho, popup model open ho". Rewrote `modules/expenses/index.php`:
    - Old layout (col-lg-5 Add Expense card + col-lg-7 list) REMOVED → page now opens with **full-width expense list** by default.
    - Top-right **"New Expense"** button (danger-red) opens Bootstrap modal `#expenseModal` (same pattern as receive_customer.php #19 / pay_supplier.php #26).
    - Modal body: Amount, Date, Category, Payment Method + Bank Account toggle, Vendor, Bill No, Description — identical fields to old inline form.
    - Filter toolbar (date from/to + category) sits above the list table, now full-width.
    - New **search bar** (`#expenseSearch`) in card header filters table rows client-side on date/category/vendor/description text.
    - Method column now shows Cash (secondary) or Bank (info) badges (same as receive/pay pages).
    - Print button + printable header block preserved (already added in item 63).
    - Verified: php -l clean; render shows New Expense button + modal + full-width list + search bar + filter toolbar. No test data (UI-only change).

65. **Purchase module rewritten for SCRAP (simple units — carton/box system removed)** - SCRAP project (`scrap_management_system`): the purchases module was copied from mehboob_traders (carton/box math) and rewritten to simple units where **qty × rate = subtotal** (NO `boxes_per_carton` / `cartons` / `loose_boxes` — `purchase_items.quantity` is plain units like kg/ton/pcs; `products.purchase_price` = rate per unit). All 8 files in `modules/purchases/` fully rewritten:
    - **`create.php`**: Party (Supplier) autocomplete (`#supplierSearch` + hidden `supplier_id`) shows name/phone/city; ac-item carries `data-plant_area`/`data-plant_name`; on pick two readonly bg-light displays `#plantArea`/`#plantName` auto-fill (supplier's plant info, "-" when empty). Purchase Date (default today), Payment Method cash/bank + bank account. Product rows: Product search | Unit (readonly from product.unit) | Quantity (`quantity[]`) | Rate (`rate[]`) | Subtotal (readonly) | Remove. Summary bar = "Total Quantity: [sum] | Total: PKR [sum]". Totals = Total (readonly)/Discount/Paid/Due (readonly). POST: subtotal=Σ(qty×rate), net=total−discount, due=net−paid; insert purchases (`invoice_no = generatePurchaseNo()`), purchase_items (quantity, purchase_price=rate, subtotal), products stock += qty + purchase_price=rate; if paid>0 recordCashOutflow/recordBankOutflow ("Purchase #INV"); updateSupplierBalance; logActivity(module 'purchase').
    - **`index.php`**: Purchase List — Invoice/Date/Supplier/Total/Paid/Due/Method/Action (View modal / Edit / Delete inline POST+confirm / Print new tab); From/To + supplier autocomplete filters; 3 stat boxes; printable header "Scrap Management System" / "Scrap Business"; view modal loads `ajax_purchase_view.php?id=`.
    - **`purchase_edit.php`**: mirrors create (incl. readonly Plant Area/Plant Name prefilled on GET); `existing_items` JSON (product_id/name/unit/quantity/rate/subtotal) prefills rows; POST reverses old stock + deletes old cash_book/bank_transactions (restores account) + delete old items, re-inserts new, recomputes `cash_book_daily` (recomputeCashDayTotals + recomputeCashDailyFrom), updates old & new supplier balances, preserves invoice_no.
    - **`purchase_print.php`**: "Scrap Management System" letterhead + PURCHASE INVOICE; supplier block (name/phone/city + Plant Area/Plant Name when set); items table `# | Product | Unit | Qty | Rate/Unit | Subtotal`; footer Total/Discount/Paid/Due; Print + Back buttons.
    - **`ajax_purchase_view.php`**: modal fragment — supplier/date/phone/city/Plant Area/Plant Name/method/status badge, items table (#/Product/Unit/Qty/Rate/Subtotal), Total/Discount/Paid/Due, Notes. Unit qty displayed trimmed (`rtrim` trailing zeros).
    - **`ajax_search.php`**: `?type=supplier` (id,name,phone,city,plant_area,plant_name,current_balance + plant fields) / `?type=product` (id,code,name,unit,purchase_price,sale_price,stock_quantity), LIKE name/phone/city and name/code, LIMIT 8, JSON.
    - **`ajax_supplier_balance.php`**: `?id=` → `updateSupplierBalance($pdo,id)` (refreshes live balance) then `formatCurrency($bal)`.
    - **`purchase_delete.php`**: POST-only; reverses stock (decimal qty, not int), deletes cash_book outflows + recomputes daily totals, deletes bank_transactions + restores account, deletes items+purchase, updateSupplierBalance, logActivity, redirect.
    - All UI English; prepared statements; `$base_url`-prefixed links; auth guard requireRole(['admin']) on every entry point.
    - Verified: php -l clean (8 files); node --check JS OK (create/edit/index); render (New Purchase + Purchase List + print letterhead); **E2E** on temp data: create 500kg @55 = 27,500 (paid 27,000, due 500, stock 100→600, supplier balance 500, cash outflow 27,000) → view modal (Plant Area/Name shown) → print page → EDIT 700kg @60 = 42,000 (stock→800, rate→60, cash 30,000, balance 12,000) → DELETE (stock→100, balance→0, items/cash rows 0). **ALL test data cleaned** (supplier, product, purchase, items, activity logs, leftover empty cash_book_daily row) — DB back to baseline (all tables 0 rows).
66. **Sales module rewritten for SCRAP (simple units, no carton/box math)** - all 13 files in `modules/sales/` rewritten from mehboob_traders (no `boxes_per_carton`; qty x rate = subtotal; `sale_items` = sale_id/product_id/quantity/price/subtotal):
    - **`index.php`** (New Sale): Customer autocomplete (name/phone/customer_no; ac-item carries `data-plant_area`/`data-plant_name`) with readonly Plant Area/Plant Name auto-fill + live balance hint (`ajax_customer_balance.php`); Salesman autocomplete (Delivered By, optional); Sale Date (flatpickr); Payment Method credit/cash/bank (credit = paid forced 0; bank shows `#bankDiv` select); product rows: Product search | Unit (readonly) | Qty | Rate | Subtotal | Remove; summary alert (Total Qty + Total); Discount/Paid/Due. POST: net=total-discount, due=net-paid, insert sales (`generateSaleNo()`, status active/completed), sale_items, stock -= qty, if paid>0 recordCashInflow/recordBankInflow ("Sale #inv", ref 'sale'), `updateCustomerBalance`, logActivity. JS guards: customer selected + >=1 product row.
    - **`invoices.php`** (Invoice List): From/To + customer autocomplete filters; per-row profit = paid_amount - SUM(purchase_price*qty) subquery; stat boxes (sales/paid/profit); view modal via iframe `invoice.php?id=`; inline POST delete; print header "SALE INVOICES".
    - **`invoice.php`** (single invoice print, GET id): "Scrap Management System" letterhead; customer block incl. Plant Area/Plant Name; items `# | Product | Unit | Qty | Rate | Subtotal` + Cost column (purchase_price*qty); footer Total/Discount/Paid/Due/Profit; "Delivered By" salesman.
    - **`sale_edit.php`**: GET prefills everything (customer + Plant Area/Name + balance, salesman, pay method, bank, `existing_items` JSON for rows). POST fully reverses/rebuilds: reverse old stock, delete old cash_book rows (collect dates) + bank_transactions (restore account), delete old items, update sales row (preserve invoice_no/created_by), re-insert items + deduct stock, record new inflow, recompute cash daily totals (recomputeCashDayTotals + recomputeCashDailyFrom), update balances. Local helper `old_qty_for()` for stock-available check. Form action = `sale_edit.php?id=` (query string REQUIRED for POST).
    - **`sale_delete.php`** (POST-only id): reverse stock, delete cash_book inflows + recompute daily, delete bank_transactions + restore account, delete items + sale, `updateCustomerBalance`, logActivity.
    - **AJAX**: `ajax_product_search.php` (id,code,name,unit,sale_price,stock_quantity, active products only), `ajax_customer_search.php` (id,full_name,phone,city,plant_area,plant_name,current_balance - **NO `status` column on customers, removed from WHERE**), `ajax_customer_balance.php` (Receivable/Advance/Clear string, redirects to index if no id), `ajax_salesman_search.php` (employees employee_type='salesman' AND status=1), `ajax_order_booker_search.php` (users role='order_booker', admin only).
    - **`packlist.php`** (Delivery Packing List): salesman autocomplete + From/To; single detail table (#, Invoice, Customer, Plant Area, Phone, Product, Qty, Amount) + summary (invoices/qty/amount); A4 `@page { size: A4 landscape }` print.
    - **`customer_summary.php`** (Customer Sales Summary): grouped by customer with rowspan customer/area/phone; Invoice/Date/Total/Paid/Due columns; per-group subtotal row (label `colspan=2` after rowspans = correct 9-col grid); grand-total tfoot (`colspan=6`); print header "Customer Sales Summary". Auth admin+order_booker.
    - **`order_booker_summary.php`** (Order Booker Sales Summary, admin only): grouped by `sales.created_by` -> users.full_name; columns Invoice/Date/Customer/Total/Paid/Due/Profit (profit = paid_amount - cost subquery SUM(p.purchase_price*si.quantity)); per-group subtotal + grand-total tfoot; From/To filters; A4 landscape print; row-level profit computed in the DISPLAY loop (not a stale copy).
    - **Roles/guards**: sales pages requireRole(['admin','order_booker']); order_booker_summary admin only; customer_summary admin+order_booker.
    - **IMPORTANT FIX (E2E root cause)**: `allocateReceiptsToSales()` was wrongly called on sale create/edit/delete - it recomputes `sales.paid_amount` from `customer_receipts` ONLY, so an upfront cash/bank-paid sale had paid wiped to 0 (due grew while balance stayed). Reference mehboob_traders calls it ONLY in `modules/transactions/receive_customer.php`. Removed the call from `index.php`, `sale_edit.php`, `sale_delete.php` (kept `updateCustomerBalance`). Verified: cash sale paid 96,000 now stays paid 96,000/due 0/completed.
    - Verified: php -l clean (13 files); render check (admin/admin123) - all pages 200, invoice/sale_edit 302 when no sale, ajax JSON OK; **E2E** on temp data: customer(TESTCUST1)+product(TESTSCRAP1,kg)+salesman -> POST sale 80kg@1200=96,000 cash paid 100k (paid capped 96,000, stock 500->420, cash inflow 96,000, invoice print ok) -> EDIT 60kg@1250=75,000 (stock 420+80-60=440, old cash replaced + new 75,000, daily totals recomputed) -> DELETE (stock back 500, sale/items/cash rows 0). **ALL test data cleaned** (customer/product/salesman/sale/items/cash_book/cash_book_daily/activity_logs) - DB back to baseline (all tables 0 rows).
67. **FINAL GLOBAL VERIFICATION (scrap project complete)** - wrap-up pass on `scrap_management_system`:
    - Global lint: ALL 71 PHP files `php -l` clean (0 syntax errors).
    - Branding grep: zero matches for Mehboob/mehboob/boxes_per_carton/scrab in *.php/*.css/*.js/*.sql (residual matches only in PROJECT_NOTES.md history, expected).
    - Render (admin/admin123 cookies): 11 key pages (index, purchase create/list, sale create, invoices, customers, suppliers, products, expenses, cashbook, bankbook) all HTTP 200 with Scrap branding present; bonus pages (pay_supplier, receive_customer, employees, salary_ledger, packlist, customer_summary, reports, profile) HTTP 200.
    - Missing-record redirects clean: invoice.php?id=1 -> invoices.php, purchase_print.php?id=1 -> index.php, supplier_view.php?id=999 -> suppliers.php, customer_receipt.php?customer_id=1 -> customers.php (all "not found" -> list page, no 500s).
    - DB verified: users 1 (admin), bank_accounts 1 (seeded), expense_categories 6 (seeded), 22 tables present, all other tables 0 rows.
    - Project ready for client hands-on: admin/admin123, first add Products -> Suppliers (with plant info) -> Purchase -> Customers -> Sale -> Receive/Pay.
68. **BRANDS REMOVED + Products relabelled Materials (scrap-appropriate UI)** - client: "brands ka scrap me koi kaam ni, scared sample tha" — removed the entire biscuit-era Brands concept and made the UI scrap-related:
    - **Brands system fully removed**: deleted `modules/inventory/brands.php` + `brand_delete.php`; sidebar "Brands" link removed; `brand_id` dropped from product create/edit forms + POST; `brand_name` LEFT JOIN removed from `products.php` list + `ajax_product_view.php` (Brand cell replaced with Unit/Purchase/Sale/Stock/MIn restructure); live DB `ALTER TABLE products DROP FOREIGN KEY products_ibfk_2; DROP COLUMN brand_id; DROP TABLE brands;` mirrored in `database_schema.sql` (brands table deleted, products FK to categories only). Grep proves zero brand references left in *.php.
    - **Products -> Materials rename (UI labels only; DB tables/columns/input names untouched)**: sidebar "Products/Add Product/Products List" -> "Materials/Add Material/Materials List"; `index.php` dashboard (All Materials, Low Stock Material column, Materials chip); inventory pages (Add/Edit Material, Material Details modal, MATERIALS LIST print header, Material Categories page title + Materials count column); purchase module (Materials section, Material label, Material column on print + view modal, "Type material name or code", "at least one material"); sales module (Material * label, Material column on invoice + packlist, "at least one material"). Placeholders made scrap-appropriate ("e.g. Iron Scrap, Copper Wire, Paper Waste"; category "e.g. Iron, Copper, Plastic").
    - **Categories seeded with 8 scrap material types** (live DB + `seed_admin.sql`): Iron & Steel, Copper, Brass, Aluminum, Plastic, Paper & Carton, Glass, Electrical (Motors/Wires).
    - **Dead sidebar link removed**: "Stock Report" pointed to `modules/inventory/stock_report.php` which never existed (in either project) -> link removed.
    - Verified: php -l clean (16 changed files); render all key pages HTTP 200, "Materials" labels present, no Brand refs; product_edit?id=1 redirects to products.php ("Material not found"); brands.php/brand_delete.php now 404; DB: categories 8 rows, brands table gone.
69. **CUSTOMER RECEIPT → INVOICE ALLOCATION + BALANCE FIX** - client: receive_customer.php shows no invoice linkage + balance inconsistent (faheem: receive page 370 due, customers.php 300 receivable, true 430). Root cause (2 compounding bugs): (a) `allocateReceiptsToSales()` recomputed ALL invoices' paid from TOTAL receipts (FIFO), overwriting sale-time payments + ignoring split across dues; (b) `updateCustomerBalance()` = opening + SUM(total-paid) - SUM(all receipts) — allocation already folds receipts into sales.paid, so receipts counted TWICE (300 instead of 430).
    - **New model**: receipts get `sales_id` (targeted allocation). Balance = `opening + SUM(total-paid_amount)` - `SUM(receipts WHERE sales_id IS NULL)` (only advances subtract; allocated receipts already live inside sales.paid).
    - **DB**: `ALTER TABLE customer_receipts ADD COLUMN sales_id INT DEFAULT NULL` + `is_split TINYINT(1) NOT NULL DEFAULT 0`; mirrored in `database_schema.sql`.
    - **functions.php**: `updateCustomerBalance()` rewritten (advance-only subtraction); `allocateReceiptsToSales()` REPLACED by incremental `allocateReceiptToSales($pdo, $customer_id, $receipt_id, $sales_id)` — targeted (specific invoice first) then FIFO continuation, tags receipt sales_id, and any excess auto-becomes an advance via an `is_split` receipt row (no cash-book entry).
    - **receive_customer.php**: modal now has "Pay Against Invoice" picker (new `ajax_customer_invoices.php` returns due-invoices for customer), POST passes `sales_id`, allocates then rebalances; history table gained "Against Invoice" column; cash/bank ledger unchanged (recorded once, split rows excluded).
    - **customer_view.php** ledger: receipts with `sales_id IS NOT NULL` skipped (already counted in that invoice's "Payment on Sale" credit) + split rows excluded -> running = current_balance.
    - **ajax_receive_payment.php** receipt now carries `sales_id`; **customer_receipt.php** calls `allocateReceiptToSales`; **reports/index.php** daily report sale-row `row_received` = `paid_amount` only (removed `+ receipts_map` double-count) + cash/bank totals exclude `is_split`.
    - **Data repair**: faheem receipt#2 (60) was mis-allocated to INV-260914-001 -> moved to INV-260914-002 (settles due 60; sale completed); sale1 paid 130->70/due 430, sale2 paid 660/due 0; customer current_balance 430. Verified via rolled-back E2E: FIFO across 2 invoices ✓, targeted fix per invoice ✓, excess->advance split (balance negative = advance) ✓, php -l clean.
